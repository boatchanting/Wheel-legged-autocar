# 遥测日志分析 (加入舵机相关量) - 2026-07-23

## 1. 整体速度表现 (Overshoot 分析)
在新的日志 `wifi_telemetry_20260723_225734_加入舵机相关.csv` 中，小车依然表现出显著的速度超调：
- **目标最大速度**：约 `3000 mm/s` (对应 `626 RPM`)
- **实际最大速度**：约 `4217 mm/s` (对应 `874 RPM`)

## 2. 舵机速度环 (Servo Speed Control) 行为剖析
通过将底层的 `target_speed`、`current_actual_speed`、`pid_servo_speed_error` 和 `pid_servo_speed_output` 结合分析，我们发现了超调的根本机制：

### 阶段A：加速起步阶段
- 目标速度突跃，触发了 **前馈加速 (Accel Feedforward)** 逻辑 (`ACCEL_FF_MODE = 1`，直接叠加 PWM)。
- 前馈 PWM 强行给轮毂电机施加了巨大的驱动力，导致车轮转速猛增。
- 为了保持平衡，直立环强制车身向前倾斜（Pitch 达到正向最大约 `4.7度`）。此时车体依靠重力倾角狂飙。

### 阶段B：超调发生阶段
- 当实际速度（`874 RPM`）超过目标速度（`626 RPM`）时，PID 的 Error 翻转。
- 舵机速度环的 PID 开始拼命往回拉，试图让车身“后坐”来减速。
- **但此时遇到了限幅**：在 `g_control_profile_accel` 中，舵机输出限幅 `servo_speed_max_output` 被定为了 `2600.0f`。PID 输出迅速触及了这个天花板，它已经尽了最大努力去压低车头/拉长前腿，但仍不足以瞬间抵消前馈带来的巨大前倾惯性。

### 阶段C：急刹车阶段 (Max Error点)
- 到了日志的第 296 步左右，路表给出的目标速度瞬间反转为 `+81 RPM`（要求倒车/急刹），而车体仍以 `-874 RPM` 的高速狂奔。
- 这导致了高达 **969 RPM** 的巨大 PID 误差。
- 此时控制组切换到了 `g_control_profile_brake`，该配置下的 `servo_speed_max_output` 阈值为 `2300.0f`。
- PID 算出的原始输出深达 `-4651`（`969 * Kp(-4.8)`），随后被死死钳制在了极限值 **-2300**。

## 3. 结论与调优建议
**结论**：舵机速度环本身没有任何 Bug，它的公式和方向计算完全正确，且在超调时已经全功率输出了最大的反制动作。问题在于**前馈过强**且**执行机构（舵机行程限幅）无法提供与之匹配的瞬间反扭矩**。

**建议的调优方向**：
1. **削弱加速前馈 (Accel FF)**：降低 `ACCEL_FF_GAIN` 或 `ACCEL_FF_MAX`，让车子加速时不要前倾得那么狠，从而减少惯性超调。
2. **放宽舵机限幅**：如果物理舵机行程允许，可以尝试将 `pid-new.c` 中的 `SERVO_SPEED_MAX_O` (包括 `accel` 和 `brake` 的 profile) 适度放大，让 PID 有更大的权限把车身“拽”回来。
3. **增加速度环 Kp**：让 PID 在误差刚出现时就更激进地介入。
