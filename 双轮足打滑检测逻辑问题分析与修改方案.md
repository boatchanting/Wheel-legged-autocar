# 双轮足打滑检测逻辑问题分析与修改方案

## 1. 分析结论

当前小半径转圈和正常过弯时的侧滑误报，根因不是单纯的侧滑阈值偏小，而是现有逻辑把一个简化的“横向加速度一致性模型”直接当成了侧滑判据。

当前判据为：

\[
e_a=a_y-v_x\omega
\]

其中：

- \(a_y\) 为重力补偿后的车体横向加速度；
- \(v_x\) 为左右轮平均得到的纵向速度；
- \(\omega\) 为偏航角差分得到的实际角速度；
- \(e_a\) 被代码直接称为异常横向加速度，并用于判断侧滑。

该判据实际上只能反映横向速度的变化率，以及传感器误差、信号延迟和模型误差，不能直接等价于车辆已经发生侧滑。因此，小半径转弯、转弯建立和退出阶段都可能产生较大的正常残差。

仅调大当前阈值可以减少误报，但同时会明显增加真正侧滑的漏检。建议先修正信号来源和重力补偿，再将瞬时阈值判断升级为带状态估计、动态不确定度和时间确认的检测机制。

---

## 2. 现有实现概述

当前代码在 [`code/navigation/inertial_nav.c`](../code/navigation/inertial_nav.c) 中执行以下计算：

```c
float centripetal_accel = v_wheel_avg * actual_yaw_rate;
float anomaly_lat_accel = acc_lat_left - centripetal_accel;
float lat_slip_thres = 500.0f + fabsf(v_wheel_avg) * 0.2f;
```

当满足：

```c
fabsf(anomaly_lat_accel) > lat_slip_thres
```

时，系统立即设置：

```c
inertial_nav.slip_flag = 1;
```

随后纵向速度融合降低轮速权重：

```c
float alpha = (inertial_nav.slip_flag == 1) ? 0.3f : NAV_ALPHA_VEL;
```

因此，误报不仅会影响遥测显示，还会直接改变惯导纵向速度的融合结果。

---

## 3. 现有逻辑的问题

### 3.1 检测量不是侧滑本身

文档使用的车体系运动学关系为：

\[
a_y=\dot v_y+v_x\omega
\]

因此：

\[
a_y-v_x\omega=\dot v_y
\]

这里得到的是横向速度变化率，而不是横向速度 \(v_y\)，也不是侧滑角 \(\beta\)。

这会造成两类问题：

1. **正常转弯误报**

   在入弯、出弯、连续弯道以及运动状态快速变化时，横向速度相关状态并不一定满足理想稳态假设，瞬时残差可能明显不为零。

2. **真实侧滑漏报**

   如果车辆已经以近似恒定的横向速度向外漂移，则可能出现：

   \[
   \dot v_y\approx 0
   \]

   此时车辆存在侧滑速度，但瞬时加速度残差可能已经减小，当前判据反而不能持续识别侧滑。

因此，现有逻辑更适合称为“横向加速度一致性检测”或“侧滑起始异常检测”，不能单独作为完整的侧滑状态检测器。

### 3.2 重力补偿可能消掉正常向心加速度

打滑检测接收到的 `acc_lat_left` 并不是未经处理的原始加速度，而是由中断调用处计算得到的重力补偿结果。以当前 `IMU_CATEGORY == 3 && CAR_SELECT == 3` 为例：

```c
9806.65f * ((float)imu_data.acc_x / 4098.0f - (float)imu_data.grav_x)
```

其中 `imu_data.grav_x` 来自姿态EKF估计。

当前姿态EKF又会将归一化后的加速度计向量作为重力方向观测：

```c
Z.data[0][0] = (matrix_type)imu_data.acc_x;
Z.data[1][0] = (matrix_type)imu_data.acc_y;
Z.data[2][0] = (matrix_type)imu_data.acc_z;
normalize_vector(&Z);
```

车辆持续转弯时，加速度计测量方向中同时包含重力和正常向心加速度。如果姿态滤波器仍将该合成方向全部解释为重力方向，可能形成以下过程：

1. 正常转弯产生真实横向向心加速度；
2. 姿态EKF逐渐将部分向心加速度解释为车身姿态变化；
3. 估计的 `grav_x` 吸收部分正常横向加速度；
4. 重力补偿后的 `acc_lat_left` 逐渐变小；
5. `v_x * actual_yaw_rate` 仍保持较大；
6. `anomaly_lat_accel` 接近 `-v_x * actual_yaw_rate`，最终超过阈值。

该问题在持续小半径转圈时尤其明显，因为车体系中的向心加速度方向可以长时间保持稳定，姿态滤波器更容易将其当成重力方向的一部分。

### 3.3 当前动态阈值没有考虑转弯半径

当前阈值为：

\[
T=500+0.2|v_x|
\]

阈值只随纵向速度增加，没有考虑角速度、转弯半径、实际向心加速度和角加速度。

正常向心加速度为：

\[
a_c=v_x\omega=\frac{v_x^2}{R}
\]

在相同速度下，转弯半径越小，\(|\omega|\) 和 \(|a_c|\) 越大，但当前阈值完全不随二者变化。这会导致小半径转弯天然比大半径转弯更容易误报。

例如：

- 纵向速度为 \(1.5\,m/s\)；
- 转弯半径为 \(0.5\,m\)；
- 正常向心加速度约为 \(4.5\,m/s^2\)；
- 当前阈值约为 \(0.8\,m/s^2\)。

此时信号幅值、时间同步或重力补偿只要产生约18%的误差，就可能触发侧滑判定。

### 3.4 实际角速度来源不适合当前公式

当前代码通过偏航角差分计算角速度：

```c
float actual_yaw_rate = diff_rad / NAV_DT;
```

这种方法存在以下问题：

- 角度差分会放大偏航角的量化噪声和短时跳变；
- 偏航角经过姿态解算及可能的航向修正，与轮速、加速度存在不同的滤波延迟；
- 10ms差分得到的平均角速度与当前采样时刻的加速度不一定时间对齐；
- 航向修正造成的小幅角度变化也会在差分后表现为角速度尖峰。

项目已经存在1ms更新的陀螺仪Z轴角速度及低通结果 `filtered_gyro_z`。对向心加速度计算而言，经过坐标轴、单位、符号和时间延迟校准后的车体系陀螺仪角速度更合适。


### 3.5 轮速、角速度和加速度没有进行时间对齐

当前残差直接组合了三种来源不同的信号：

- 电机驱动器反馈的左右轮速；
- 姿态角差分得到的角速度；
- IMU及姿态EKF输出的重力补偿加速度。

这些信号的刷新频率、内部滤波和通信延迟不同。在转弯建立阶段，可能出现：

- 轮速已经发生变化；
- 陀螺仪和机身运动稍后响应；
- 姿态角差分又比陀螺仪增加额外延迟；
- 重力补偿加速度具有另一套滤波相位。

即使所有传感器稳态值都正确，直接在同一时刻相减仍然可能得到较大的瞬时残差。

### 3.6 单帧判定缺少去抖和迟滞

当前逻辑中，任意一个10ms采样超过阈值就立即设置 `slip_flag = 1`，低于阈值又立即恢复正常。

当前判断没有：

- 连续超限时间确认；
- 进入和退出双阈值；
- 候选侧滑状态；
- 传感器质量检查；
- 转弯瞬态的不确定度调整。

因此，正常转弯中的短时尖峰也会直接表现为侧滑状态。

### 3.7 横向速度积分使用了错误的输入量

当前代码直接积分 `acc_lat_left`：

```c
inertial_nav.vy_body =
    inertial_nav.vy_body * 0.95f + acc_lat_left * NAV_DT;
```

但是正常转弯时，`acc_lat_left` 本身包含正常向心加速度。直接积分会把正常转弯积累成虚假的横向速度。

横向速度估计应积分去除正常转弯分量后的残差：

\[
e_a=a_y-v_x\omega
\]

同时需要偏置估计、泄漏衰减、零速重置和限幅，避免加速度计零偏长期积累。

### 3.8 坐标系和文档配置存在不一致

当前源码、头文件注释和说明文档中，前进速度正负方向的描述并不完全一致。例如 `inertial_nav.h` 将 `vx_body` 注释为前进为正，而部分实际速度换算及其他控制模块按照前进为负处理。

这不一定是本次误报的直接原因，但会增加以下风险：

- 左右转残差极性不一致；
- 更换车型或IMU后符号错误；
- 轮速向心加速度与IMU横向轴方向相反；
- 调参时通过绝对值掩盖了坐标错误。

此外，[`docs/双轮足打滑检测机制说明.md`](双轮足打滑检测机制说明.md) 中写的是：

```c
#define SLIP_DETECTION_ENABLE 1U
```

但当前 [`code/config/sys_options.h`](../code/config/sys_options.h) 中实际配置为：

```c
#define SLIP_DETECTION_ENABLE 0U
```

需要先确认实车烧录固件对应的源码版本和编译开关，避免文档、当前工作区和实车行为不一致。

---

## 4. 修改方案

### 4.1 第一阶段：修正现有横向加速度一致性检测

该阶段保留现有总体结构，优先解决正常转弯误报。

#### 4.1.1 使用车体系陀螺仪角速度

向心加速度计算中的 \(\omega\) 应改用经过以下处理的车体系陀螺仪角速度：

1. 校准陀螺仪零偏；
2. 明确车辆坐标系与IMU坐标系的轴向映射；
3. 统一为 `rad/s`；
4. 确认左右转符号；
5. 进行适当低通滤波；
6. 与轮速及横向加速度对齐到相同采样时刻。

偏航角差分可以继续用于位置和航向积分，但不建议继续作为向心加速度模型中的瞬时角速度来源。

#### 4.1.2 改善动态状态下的重力估计

姿态EKF不应在明显转弯时无条件把加速度计合成方向作为纯重力方向。

可选择以下方案之一：

1. **自适应加速度计观测权重**

   当以下任一条件较大时，提高加速度计观测噪声，降低其对姿态的修正权重：

   - \(|v_x\omega|\)；
   - 加速度模长与 \(g\) 的偏差；
   - 转向命令；
   - 角速度或角加速度。

   在直线、低动态状态下再恢复正常加速度计权重。

2. **先去除预测运动加速度再估计重力**

   在使用加速度计更新重力方向前，先减去根据轮速、陀螺仪和纵向状态预测的运动加速度，再对剩余向量进行归一化和姿态校正。

3. **短时陀螺仪主导、稳定状态回正**

   转弯期间主要依赖陀螺仪传播姿态，转弯结束且加速度恢复稳定后，再使用加速度计缓慢修正长期漂移。

#### 4.1.3 将IMU加速度补偿到轮轴参考点

测量IMU相对左右轮轴中心的位置向量 \(\boldsymbol r\)，使用角速度和角加速度补偿：

\[
\boldsymbol a_{axle}
=
\boldsymbol a_{imu}
-
\dot{\boldsymbol\omega}\times\boldsymbol r
-
\boldsymbol\omega\times
(\boldsymbol\omega\times\boldsymbol r)
\]

然后使用轮轴参考点的横向加速度计算一致性残差。

如果暂时不进行完整补偿，至少应将 \(|\dot\omega|\) 和 \(|\omega|^2\) 纳入模型不确定度或动态阈值。

#### 4.1.4 对所有输入信号进行时间对齐

建议统一在100Hz检测周期中使用经过时间对齐的：

- 左右轮速；
- 车体系陀螺仪角速度；
- 轮轴参考点横向加速度。

可通过正常左右转弯日志计算信号互相关，估计各信号之间的固定延迟。延迟较小的信号应增加相应延迟或使用相同截止频率的滤波器，避免把相位差当成侧滑。

#### 4.1.5 使用与转弯强度相关的阈值

简化的动态阈值可写为：

\[
T_a=
A_0
+k_c|v_x\omega|
+k_\alpha|\dot\omega|
+k_{\omega2}|\omega|^2
\]

其中：

- \(A_0\)：覆盖直行时的加速度噪声、零偏和轻微振动；
- \(k_c|v_x\omega|\)：覆盖向心加速度计算的比例误差；
- \(k_\alpha|\dot\omega|\)：覆盖入弯和出弯的动态误差；
- \(k_{\omega2}|\omega|^2\)：覆盖IMU安装位置和高角速度下的模型误差。

更推荐使用归一化创新或类似卡方判定的形式：

\[
J_a=
\frac{(a_y-v_x\omega)^2}
{\sigma_a^2+\omega^2\sigma_v^2+v_x^2\sigma_\omega^2+\sigma_{model}^2}
\]

其中 \(\sigma_{model}\) 可随 \(|\dot\omega|\)、\(|\omega|^2\) 和传感器质量动态变化。

这种方法不会因为转弯半径变小而仍使用几乎不变的绝对阈值。

#### 4.1.6 增加状态机和迟滞

建议将侧滑判断改为：

```text
NORMAL -> CANDIDATE -> SLIP
```

初始时间参数可从以下范围开始测试：

- 残差超过进入阈值持续80～150ms，进入 `CANDIDATE`；
- 候选状态继续满足条件后，确认 `SLIP`；
- 退出使用低于进入阈值的独立阈值；
- 连续正常250～500ms后才退出侧滑状态。

进入阈值和退出阈值必须分开，避免状态在阈值附近反复跳变。

原地自转状态也应优先使用左右轮速度和、速度差以及陀螺仪角速度共同识别，不能只依赖平均轮速接近零。

---

### 4.2 第二阶段：估计横向速度和侧滑角

修正并时间对齐信号后，计算：

\[
e_a=a_{y,axle}-v_x\omega_z
\]

使用带泄漏和偏置修正的积分器估计横向速度：

\[
\hat v_y(k)
=
\lambda\hat v_y(k-1)
+
(e_a-b_a)\Delta t
\]

其中：

- \(\lambda\) 为接近1的泄漏系数；
- \(b_a\) 为横向加速度残差偏置估计；
- 静止状态下应将 \(\hat v_y\) 重置为零；
- 应对 \(\hat v_y\) 和偏置设置合理限幅。

进一步计算侧滑角：

\[
\hat\beta
=
\arctan\left(
\frac{\hat v_y}
{\max(|v_x|,v_{min})}
\right)
\]

侧滑判定不再只看一个瞬时加速度样本，而是综合：

- 归一化横向残差 \(J_a\)；
- 横向速度估计 \(|\hat v_y|\)；
- 侧滑角估计 \(|\hat\beta|\)；
- 条件持续时间；
- 当前传感器和模型可信度。

初始侧滑角阈值可从以下范围开始测试：

- 进入阈值：5°～8°；
- 退出阈值：2°～4°。

低速时侧滑角分母过小，不应继续使用正常的侧滑角阈值，应切换到低速横向异常事件检测。

---

### 4.3 第三阶段：将轮速角速度差作为辅助证据

不建议完全删除轮速推算角速度。原有方案的问题主要是没有处理机械响应、滤波和采样带来的相位差，而不是该信息完全无效。

轮速理论角速度为：

\[
\omega_{wheel}
=
\frac{v_R-v_L}{B_{eff}}
\]

辅助残差可定义为：

\[
e_\omega
=
\omega_{gyro}
-
\operatorname{DelayOrFilter}(\omega_{wheel})
\]

建议：

1. 分别校准左右轮速度比例；
2. 标定有效轮距 \(B_{eff}\)，不要只使用机械测量值；
3. 使用正常转弯数据测量轮速角速度和陀螺仪角速度的相位差；
4. 对两路信号使用一致的带宽和采样周期；
5. 转弯建立阶段放宽角速度残差，稳态阶段适当收紧；
6. 将其作为侧滑置信度的辅助项，而不是单独的一票否决条件。

轮速角速度差可以帮助识别轮胎空转、偏航响应异常或模型失配，但纯横向漂移不一定伴随明显角速度差，因此不能要求所有侧滑都必须同时满足该条件。

---

## 5. 传感器可观测性限制

仅依赖轮速、陀螺仪和加速度计，无法可靠观测持续的恒定横向速度。

原因是加速度计只能测量速度变化，无法直接测量匀速横向移动。如果车辆侧滑已经进入近似恒定状态，则可能出现：

\[
\dot v_y\approx 0
\]

此时单纯依赖加速度残差不能持续确认车辆正在横向漂移。

如果任务要求可靠检测持续侧滑，需要增加至少一种独立横向速度或位置观测，例如：

- GNSS速度矢量；
- 地面光流；
- 摄像头视觉里程计；
- 赛道横向偏差及其变化率；
- 外部定位系统。

如果不增加独立观测，算法名称和功能边界应定义为“侧滑起始或横向异常检测”，不应认为其能够覆盖所有持续侧滑状态。

---

## 6. 参数标定与实车验证方案

### 6.1 建议记录的数据

至少记录以下变量，并确保使用统一时间戳：

- 三轴原始加速度；
- EKF估计的三轴重力分量；
- 重力补偿后的横向加速度；
- 补偿到轮轴中心后的横向加速度；
- 左右轮原始速度和换算后的线速度；
- 左右轮平均速度；
- 原始及滤波后的陀螺仪Z轴角速度；
- 偏航角差分角速度；
- 轮速理论角速度；
- \(v_x\omega\)；
- 横向残差 \(e_a\)；
- 归一化残差 \(J_a\)；
- 横向速度估计 \(\hat v_y\)；
- 侧滑角估计 \(\hat\beta\)；
- 候选侧滑状态和最终侧滑状态。

### 6.2 建议测试工况

左右两个方向都应分别测试：

1. 静止状态；
2. 不同速度直线行驶；
3. 大半径稳态圆周；
4. 小半径稳态圆周；
5. 普通入弯和出弯；
6. 连续S弯；
7. 原地自转；
8. 快速转向命令但车辆仍正常抓地；
9. 纸片横向拖拽；
10. 不同路面摩擦系数下的真实侧滑。

### 6.3 建议的分析顺序

1. **检查坐标和符号**

   正常左转和右转时，确认 `acc_lat_left` 与 `v_x * omega` 的符号是否一致。

2. **检查重力估计污染**

   在稳态圆周中观察：

   - `v_x * omega` 是否稳定；
   - 原始横向加速度是否稳定；
   - `grav_x` 是否逐渐变化；
   - 重力补偿后的横向加速度是否逐渐衰减。

   如果后两项出现明显变化，可以确认姿态EKF正在吸收正常向心加速度。

3. **检查信号延迟**

   对轮速角速度、陀螺仪角速度和横向加速度计算互相关，得到入弯和出弯时的相对延迟。

4. **建立正常工况统计范围**

   使用全部正常直线和转弯数据，统计不同速度、角速度和角加速度下残差的均值、标准差和高分位数。

5. **确定动态阈值和状态机时间**

   阈值应覆盖正常工况的高分位残差，同时保证真实侧滑数据能够持续进入候选状态。

6. **独立验证**

   参数确定后，应使用未参与调参的数据重新测试，避免阈值只对某一条路线或某一种速度有效。

---

## 7. 推荐实施优先级

建议按以下顺序推进：

1. 统一车辆坐标系、轮速和IMU轴向符号；
2. 使用车体系陀螺仪角速度替代偏航角差分角速度；
3. 解决转弯时姿态EKF将向心加速度吸收到重力估计的问题；
4. 对轮速、角速度和横向加速度进行滤波及时间对齐；
5. 测量并补偿IMU相对轮轴中心的位置；
6. 将固定阈值改为与 \(|v_x\omega|\)、\(|\dot\omega|\) 和 \(|\omega|^2\) 有关的动态阈值或归一化创新；
7. 增加候选状态、持续时间确认和退出迟滞；
8. 将 `vy_body` 的积分输入改为横向残差，而不是原始横向加速度；
9. 增加横向速度和侧滑角估计；
10. 将时间对齐后的轮速角速度差重新作为辅助证据；
11. 如需可靠检测持续侧滑，引入独立横向速度或位置观测。

其中第2、3、4、6、7项是解决正常小半径转弯误报的关键；只调整当前的 `500 + 0.2 * |v|` 阈值不建议作为最终方案。
