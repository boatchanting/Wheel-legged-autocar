# 循迹系统升级任务书：连续S弯高速绕桩与防切弯完整优化方案

**任务背景：**
当前使用纯追踪（Pure Pursuit）算法在连续 S 弯（绕桩）时存在严重的“切弯”导致撞桩的缺陷。此前为了防撞，上位机扩大了避障余量，导致轨迹曲率增大，触发了离线速度规划的限速，极大限制了车辆极速。
**升级目标：** 
1. **上位机：** 压榨安全余量，生成曲率最小的“极速赛车线”，彻底解放离线速度上限。
2. **下位机：** 引入 CTE（横向误差）补偿算法（Stanley思想）防切弯，并修复前瞻曲率盲区，实现高速且精准的绕桩。

---

## 第一部分：上位机（PC端）修改 —— 压榨轨迹空间，解放速度上限

**涉及文件：** `tools/webview_nav_marker速度规划/calculate_path.py`
**操作目的：** 缩小避障安全余量，使轨迹在绕桩时尽可能直（曲率极小），从而在 `apply_speed_plan` 阶段获得极高的目标速度。

**修改步骤：**
打开脚本，找到约第 36 行全局参数配置区，修改 `SAFE_MARGIN_MM` 的值：

```python
# ============================================================
# 自动轨迹解算专属参数
# ============================================================
CAR_HALF_WIDTH_MM = 135.0      # 车身半宽
CONE_RADIUS_MM = 140.0         # 桩桶物理半径

# 【修改这里】：将原本的 200.0 修改为 50.0 甚至 30.0
# 目的：极度压榨绕桩余量，降低 S 弯曲率以提升离线规划速度上限
SAFE_MARGIN_MM = 50.0          

CONE_OFFSET_MM = CAR_HALF_WIDTH_MM + CONE_RADIUS_MM + SAFE_MARGIN_MM  # 桩桶横向偏置距离
```
*📌 执行人注意：修改后请重新运行 Python 脚本生成新的路表头文件。在生成的预览图中，蓝色轨迹应极其贴近橙色的桩桶碰撞圈。*

---

## 第二部分：下位机（MCU端）算法升级

**涉及文件：** 
1. `code/navigation/nav_replay/plan1/plan1_pure_pursuit_speed_planning.h`
2. `code/navigation/nav_replay/plan1/plan1_pure_pursuit_speed_planning.c`

### 修改点 1：新增横向纠偏增益参数
打开 `plan1_pure_pursuit_speed_planning.h`，在参数配置区（约 30 行附近）新增参数：

```c
// 原有参数
#define KAPPA_CURVE_BYPASS_THRESH  0.0005f  // 弯道旁路曲率阈值
#define K_FF_CURVATURE             100.0f   // 曲率前馈增益

// 【新增参数】：横向误差纠偏增益 (Stanley补偿增益)
// 值越大，车身偏离轨迹时拉回来的力度越猛，需结合实车调试
#define PP_STANLEY_K               45.0f    
```

### 修改点 2：修复 S 弯前瞻曲率盲区（包含防崩溃下限保护）
打开 `plan1_pure_pursuit_speed_planning.c`，找到**阶段6a (前瞻距离计算)** 约 636-643 行。

**原逻辑缺陷：** 固定读取前方第15个点，在S弯交界处会读到曲率0，导致前瞻距离暴增，引发切弯甚至失控。
**替换为以下逻辑（滑动窗口最大曲率扫描 + 兜底保护）：**

```c
    // --- 【修改开始】: 修复 S 弯曲率盲区 ---
    float max_ahead_kappa = 0.0f;
    int search_end = base_idx + 30; // 往前看约 1.5 米（按点间距50mm估算）
    if (search_end > stop_idx) {
        search_end = stop_idx;      // 不得越过停车屏障
    }
    
    // 扫描前方窗口内的最大曲率，确保在 S 弯中间前瞻距离依然收紧
    for (int i = base_idx + 5; i <= search_end; i++) {
        float k = fabsf(nav_ram_data.points[i].curvature);
        if (k > max_ahead_kappa) {
            max_ahead_kappa = k;
        }
    }
    
    // 用扫描到的最大曲率计算前瞻缩减因子
    float ahead_kappa = max_ahead_kappa;
    float shrink_factor = 1.0f - ahead_kappa * PP_LD_KAPPA_SHRINK_GAIN;
    
    // 【必须保留的兜底保护】：防止急弯时 shrink_factor 变负数导致前瞻崩溃
    if (shrink_factor < PP_LD_KAPPA_SHRINK_MIN) {
        shrink_factor = PP_LD_KAPPA_SHRINK_MIN;
    }
    // --- 【修改结束】 ---
```

### 修改点 3：注入 CTE（横向误差）补偿核心代码
继续在 `plan1_pure_pursuit_speed_planning.c` 中，找到**阶段6c (转向角计算)**，在纯追踪计算后、旁路滤波之前插入代码。

**替换对应的区块：**

```c
        // 原始纯追踪基础计算
        float target_yaw = -atan2f(ty - y, -(tx - x)) * 57.29578f;  
        raw_err_degree = NormalizeAngle(target_yaw - relative_yaw);

        // --- 【新增代码区块开始】: CTE 横向误差补偿 (Stanley思想) ---
        NavRamPoint_t closest_pt = nav_ram_data.points[base_idx];
        
        // 1. 获取最近点的轨迹切向方向 (世界坐标)
        float path_rad = closest_pt.target_yaw_deg * 0.0174533f; // 转为弧度
        float path_dir_x = cosf(path_rad);
        float path_dir_y = sinf(path_rad);
        
        // 2. 计算车身中心指向最近点的位置误差向量
        float err_vec_x = closest_pt.x - inertial_nav.x;
        float err_vec_y = closest_pt.y - inertial_nav.y;
        
        // 3. 向量叉乘计算带符号的横向误差 CTE (Cross-Track Error)
        // 叉乘 = (path_dir) X (err_vec)
        float cross_product = path_dir_x * err_vec_y - path_dir_y * err_vec_x;
        float cte = sqrtf(err_vec_x * err_vec_x + err_vec_y * err_vec_y);
        
        // 根据叉乘结果赋予符号
        if (cross_product < 0.0f) {
            cte = -cte; 
        }
        
        // 4. 计算当前绝对车速 (加入最低限速防止原地抽搐或除零异常)
        float current_speed = fabsf(inertial_nav.vx_body);
        if (current_speed < 100.0f) {
            current_speed = 100.0f; 
        }
        
        // 5. 计算补偿角度并叠加
        // 注意：后续实车测试时，若纠偏方向反了，请在 PP_STANLEY_K 前加负号
        float stanley_comp_deg = atan2f(PP_STANLEY_K * cte, current_speed) * 57.29578f; 
        raw_err_degree = NormalizeAngle(raw_err_degree + stanley_comp_deg);
        // --- 【新增代码区块结束】 ---
        
        // 下面接原有的旁路滤波逻辑...
        if (fabsf(target_kappa) > KAPPA_CURVE_BYPASS_THRESH) {
            // ... (保持不变)
```

---
