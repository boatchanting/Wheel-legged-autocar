# 双轮足机器人惯导系统优化技术方案

> **文档版本**: v1.0  
> **生成日期**: 2026-07-22  
> **适用工程**: 双轮足并联腿自平衡机器人  
> **参考来源**: Seekfree CYT4BB 飞控 IMU 滤波技术文档

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [当前惯导方案详细分析](#2-当前惯导方案详细分析)
3. [飞控IMU方案技术参考](#3-飞控imu方案技术参考)
4. [改进方案设计](#4-改进方案设计)
5. [分步实施计划](#5-分步实施计划)
6. [验收标准与测试方法](#6-验收标准与测试方法)

---

## 1. 项目背景与目标

### 1.1 背景

当前双轮足机器人的惯导系统存在以下问题：
- 长时间运行后位置/航向漂移明显
- 动态加速时姿态估计可能被加速度干扰
- 温度变化导致陀螺仪零偏变化，影响精度
- 缺乏在线自适应校正能力

### 1.2 优化目标

| 指标 | 当前水平 | 目标水平 |
|------|---------|---------|
| 静态航向漂移 | 持续漂移 | < 1°/min |
| 动态姿态稳定性 | 加速时可能抖动 | 全程稳定 |
| 温度适应性 | 温度变化后需重新标定 | 自动补偿 |
| 长时间运行 | 需手动重新标定 | 自动跟踪零偏 |

### 1.3 技术路线概述

采用飞控验证过的成熟方案，分阶段实施：
1. EKF自适应R + Acc Trust门控（核心改进）
2. 温度漂移补偿
3. 在线零偏学习
4. 滤波器链优化（可选）

---

## 2. 当前惯导方案详细分析

### 2.1 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        当前系统架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  IMU硬件 (IMU660RA/RB/963RA)                                    │
│      │                                                           │
│      ├──→ ekf.c: 四元数EKF姿态估计                               │
│      │       │                                                   │
│      │       ├── 输入: 陀螺仪(rad/s) + 加速度计(g)               │
│      │       ├── 状态: 四元数 [w,x,y,z] (4维)                    │
│      │       ├── 预测: 陀螺仪积分 (1ms)                          │
│      │       ├── 更新: 加速度计观测重力方向 (1ms)                 │
│      │       └── 输出: euler_angle (roll/pitch/yaw)              │
│      │                                                           │
│      └──→ inertial_nav.c: 惯性导航解算                           │
│              │                                                   │
│              ├── 输入: euler_angle.yaw + 轮速 + acc + gyro_z     │
│              ├── 周期: 10ms                                      │
│              ├── 融合: alpha加权 (轮速 vs acc积分)                │
│              └── 输出: x, y, relative_yaw, vx_body, vy_body      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 EKF实现细节 (ekf.c)

#### 2.2.1 状态向量

```c
// 四元数状态
matrix_t exf_x;  // [q0, q1, q2, q3] = [w, x, y, z]
```

#### 2.2.2 噪声矩阵配置

| 矩阵 | 当前值 | 说明 |
|------|--------|------|
| **Q (过程噪声)** | `0.001 * I(4×4)` | 固定，偏小 |
| **R (观测噪声)** | `10000 * I(3×3)` | 固定，**极大** |
| **P初始** | `1000000 * I(4×4)` | 极大 |

**问题分析**：
- R=10000 意味着加速度计观测噪声方差为10000，标准差约100
- 而加速度计归一化后的观测值量级约为1.0
- 创新 (innovation) 量级约0.01，`innovation²/R ≈ 1e-8`
- **结果**：卡尔曼增益K极小，加速度计几乎不参与校正，EKF退化为纯陀螺仪积分

#### 2.2.3 数据预处理

```c
void imu_get_values(void) {
    // 1. 获取原始数据
    IMU_GET_GYRO();
    IMU_GET_ACC();
    
    // 2. 减去启动时标定的零偏
    float gx_temp = (float)IMU_GYRO_X - gyro_offset_x;
    // ...
    
    // 3. 死区处理 (GYRO_DEAD_ZONE = 8.0 原始单位)
    if (fabs(gx_temp) < GYRO_DEAD_ZONE) gx_temp = 0.0f;
    
    // 4. 单位转换 (原始值 → rad/s)
    imu_data.gyro_x = gx_temp * PI / 180 / 16.384f;  // IMU660RA
    // 或
    imu_data.gyro_x = gx_temp * PI / 180 / 14.3f;    // IMU963RA
    
    // 5. 加速度计一阶低通滤波
    imu_data.acc_x = K * IMU_ACC_X + (1 - K) * IMU_ACC_X_LP;
}
```

**问题分析**：
- 陀螺仪零偏只在启动时标定一次，之后不变
- 死区阈值8.0（原始单位）≈ 0.004 rad/s，可能导致小角速度被截断
- 加速度计低通滤波系数K未在代码中明确定义

#### 2.2.4 EKF更新流程

```c
void EKF_UpData(void) {
    // 1. 获取数据
    imu_get_values();
    
    // 2. 加速度计归一化作为观测
    Z = [acc_x, acc_y, acc_z];
    normalize_vector(&Z);
    
    // 3. 状态预测 (陀螺仪积分)
    F = 四元数微分方程的一阶近似;
    exf_x = F * exf_x;
    normalize_vector(&exf_x);
    
    // 4. 协方差预测
    P = F * P * F' + Q;
    
    // 5. 卡尔曼增益计算
    H = 观测雅可比矩阵;
    S = H * P * H' + R;
    K = P * H' * inv(S);
    
    // 6. 误差门控 (error = e' * S⁻¹ * e)
    if (error > r_yz) { return; }  // r_yz = 0.001
    
    // 7. 状态更新
    exf_x = exf_x + K * innovation;
    normalize_vector(&exf_x);
    
    // 8. 协方差更新 (简单形式)
    P = (I - K*H) * P;
    
    // 9. 提取重力分量 + 转换为欧拉角
}
```

**问题分析**：
- 误差门控阈值 `r_yz = 0.001` 过于严格，可能拒绝大部分acc校正
- 协方差更新使用简单形式 `(I-KH)P`，数值稳定性不如Joseph形式
- 没有Acc Trust机制，动态时acc会污染姿态

### 2.3 惯性导航实现细节 (inertial_nav.c)

#### 2.3.1 融合逻辑

```c
void InertialNav_Update(...) {
    // 1. 轮速转换
    float v_L_mm = -speed_L * SPEED_TO_MM_S;
    float v_R_mm = speed_R * SPEED_TO_MM_S;
    float v_wheel_avg = (v_L_mm + v_R_mm) * 0.5f;
    
    // 2. 打滑检测
    float theoretical_yaw_rate = (v_R - v_L) / WHEEL_BASE;
    float actual_yaw_rate = gyro_z_rad_s;
    // 差值 > 0.5 rad/s 视为打滑
    
    // 3. 纵向速度融合
    float alpha = (slip_flag == 3) ? 0.1f : 1.0f;  // 雷区自转时0.1，否则1.0
    float v_pred = vx_body + acc_lon * NAV_DT;
    vx_body = alpha * v_wheel_avg + (1-alpha) * v_pred;
    
    // 4. 横向速度衰减
    vy_body *= 0.8f;  // 非完整约束，快速衰减
    
    // 5. 坐标变换与积分
    vx_world = vx*cos(yaw) - vy*sin(yaw);
    vy_world = vx*sin(yaw) + vy*cos(yaw);
    x += vx_world * NAV_DT;
    y += vy_world * NAV_DT;
}
```

#### 2.3.2 关键参数

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `NAV_DT` | 0.01s | 导航解算周期 |
| `NAV_ALPHA_VEL` | 1.0 | 正常行驶轮速权重(100%) |
| `YAW_RATE_DIFF_THRES` | 0.5 rad/s | 打滑检测阈值 |
| `WHEEL_BASE_MM` | 185mm | 新车轮距 |
| `SPEED_TO_MM_S` | 4.866666 * 1.065 | 轮速脉冲→mm/s系数 |

**问题分析**：
- 正常行驶时 `alpha=1.0`，完全不使用加速度计，无法检测/修正漂移
- 打滑检测只比较角速度，漏检直线打滑
- 横向速度简单衰减，无融合观测
- 无温漂补偿和在线学习

### 2.4 零点标定流程

```c
void IMU_Calibrate_All_Gyro(void) {
    const int sample_count = 2000;
    for (int i = 0; i < sample_count; i++) {
        IMU_GET_GYRO();
        IMU_GET_ACC();
        sum_x += IMU_GYRO_X;
        // ...
    }
    gyro_offset_x = sum_x / sample_count;
    acc_offset_x = sum_x_a / sample_count;
    // ...
}
```

**问题分析**：
- 只在启动时执行一次
- 没有静止检测门控（假设启动时是静止的）
- 没有温度记录
- 没有姿态角零点标定

---

## 3. 飞控IMU方案技术参考

### 3.1 飞控系统概述

| 参数 | 飞控配置 | 本工程配置 |
|------|---------|-----------|
| MCU | Cortex-M7 @ 250MHz | 类似 |
| IMU | 双ICM-42688-P | 单IMU660RA/RB/963RA |
| Gyro采样率 | 2kHz | ~1kHz |
| Acc采样率 | 416Hz | ~400Hz |
| 控制周期 | 500us/1ms | 1ms/10ms |

### 3.2 飞控EKF核心设计

#### 3.2.1 四元数EKF参数

| 参数 | 飞控值 | 本工程当前值 | 说明 |
|------|--------|-------------|------|
| `Q_QUAT_CONT` | 2.0e-5 | 0.001 (等效) | 过程噪声密度 |
| `R0` | 0.004 | 10000 | 基础观测噪声 |
| `R_MIN` | 0.001 | - | R下界 |
| `R_MAX_NORMAL` | 0.08 | - | 正常R上界 |
| `R_MAX_DYNAMIC` | 0.80 | - | 动态R上界 |
| `R_ALPHA` | 30.0 | - | R放大系数 |
| `P_INIT` | 0.02 | 1000000 | 初始协方差 |
| `CHI2_GATE` | 9.0 | 0.001 (error) | 门控阈值 |

#### 3.2.2 自适应R机制

```c
// 飞控实现
float a_err_g = fabsf(acc_norm / 9.81f - 1.0f);  // 加速度模长偏差
float r_adapt = R0 * (1.0f + R_ALPHA * a_err_g * a_err_g);
float r_eff = fclip(r_adapt, R_MIN, R_MAX_NORMAL);

// 动态低trust模式时
r_eff = fclip(r_adapt, R_MIN, R_MAX_DYNAMIC);  // 上限放宽到0.80
```

**设计原理**：
- 静止时 `a_err ≈ 0`，`R ≈ R0 = 0.004`，acc高度可信
- 加速时 `a_err` 增大，R快速增大，降低acc权重
- 非线性放大 (`a_err²`) 对大偏差更敏感

#### 3.2.3 Acc Trust三维门控

```c
// 三个独立信任分量
float trust_norm = ramp_down(a_err_g, 0.03f, 0.18f);      // 模长信任
float trust_innov = ramp_down(innov_norm, 0.035f, 0.22f);  // 创新信任
float trust_xy = ramp_down(innov_xy, 0.030f, 0.20f);       // 水平创新信任

// 综合信任度
float acc_trust = trust_norm * trust_innov * trust_xy;

// 门控决策
if (acc_trust < 0.08f) {
    // 跳过本帧校正
    skip_correct();
    return;
}

// ramp_down函数
float ramp_down(float value, float soft, float hard) {
    if (value <= soft) return 1.0f;
    if (value >= hard) return 0.0f;
    return 1.0f - (value - soft) / (hard - soft);
}
```

**设计原理**：
- **模长信任**：`|acc| - 1g|` 越大，越可能是机动而非纯重力
- **创新信任**：预测与观测差异越大，越可能是模型不匹配
- **水平创新**：水平方向创新异常更敏感（重力主要在Z轴）
- 三者相乘，任一维度异常都会降低信任

#### 3.2.4 动态保持策略

| 级别 | 保持时间 | R上限 | 触发条件 |
|------|---------|-------|---------|
| Soft | 0ms | 0.08 | trust略低，只通过R降权 |
| Hard | 30ms | 0.80 | acc模长或创新进入hard区 |
| Severe | 90ms | 0.80 | acc_trust极低或连续reject |

```c
if (acc_trust >= 0.08f) {
    hold_ms = 0;   // 正常校正
} else if (进入hard区) {
    hold_ms = 30;  // 短保持，跳过几帧
} else {
    hold_ms = 90;  // 长保持，允许弱校正
}
```

#### 3.2.5 Chi2门控

```c
#define CHI2_GATE 9.0f  // 约3σ

// 逐轴检查
for (int i = 0; i < 3; i++) {
    float nis = innovation[i] * innovation[i] / S[i][i];
    if (nis > CHI2_GATE) {
        reject[i] = 1;
        R[i] *= 50.0f;  // 膨胀R，降低该轴权重
        reject_count[i]++;
    }
}

// 全轴拒绝则跳过
if (reject[0] && reject[1] && reject[2]) return;
```

#### 3.2.6 协方差更新 (Joseph形式)

```c
// 飞控使用Joseph形式，数值更稳定
matrix_t A = I - K * H;
P = A * P * A' + K * R * K';

// 对比本工程当前的简单形式
P = (I - K*H) * P;  // 可能导致P不对称、非正定
```

### 3.3 飞控滤波器链设计

#### 3.3.1 多路径架构

```
原始Gyro (2kHz)
    │
    ├── [减静态bias]
    ├── [减在线bias]
    ├── [温漂补偿]
    │
    ▼
┌───────────────────────────────────────┐
│ 动态Notch (TPT-SVF, 级联3槽)          │
│   Slot0: 基频 (油门前馈, 55-280Hz)    │
│   Slot1: 2x谐波 (FFT确认后启用)       │
│   Slot2: 3x谐波 (FFT确认后启用)       │
├───────────────────────────────────────┤
│ 静态Notch (直接型IIR, 默认关闭)        │
└─────────────────┬─────────────────────┘
                  │
    ┌─────────────┼─────────────┬─────────────┐
    ▼             ▼             ▼             ▼
EKF预测      BW2 130Hz     PT2 50-70Hz    PT1 60Hz
(直通)       (速率P/I)     (速率D)        (姿态阻尼)
```

**设计哲学**：
- 噪声前置切除（Notch在最前面）
- 多路径分工（不同控制环用不同截止频率）
- 动态自适应（Notch中心频率随油门变化）

#### 3.3.2 可借鉴的滤波器

| 滤波器 | 用途 | 本工程适用性 |
|--------|------|-------------|
| PT1 (一阶低通) | 姿态阻尼 | ⭐⭐⭐⭐ Gyro进入EKF前 |
| PT2 (二阶低通) | D项滤波 | ⭐⭐⭐ 轮速平滑 |
| BW2 (Butterworth) | P/I项滤波 | ⭐⭐⭐ 加速度滤波 |
| Notch (陷波) | 固定频点抑制 | ⭐⭐ 电机噪声（如有） |

### 3.4 飞控温漂补偿方案

#### 3.4.1 离线拟合流程

```
1. 将机器人放入温箱（或自然降温/升温环境）
2. 静止状态下，从高温到低温（或反向）采集数据
3. 每隔0.5°C记录一个样本点：
   - IMU温度 (degC)
   - 陀螺仪三轴均值 (rad/s)
4. 线性拟合：gyro_bias = slope * (temp - ref_temp) + intercept
5. 得到三轴斜率：SLOPE_X, SLOPE_Y, SLOPE_Z
```

#### 3.4.2 在线补偿

```c
// 启动时记录参考温度
float bias_temp = imu_temperature;  // 标定时的温度

// 运行时实时补偿
float delta_temp = current_temp - bias_temp;
float correction_x = fclip(SLOPE_X * delta_temp, -0.05f, 0.05f);
float correction_y = fclip(SLOPE_Y * delta_temp, -0.05f, 0.05f);
float correction_z = fclip(SLOPE_Z * delta_temp, -0.05f, 0.05f);

gyro_zeroed_x -= correction_x;
gyro_zeroed_y -= correction_y;
gyro_zeroed_z -= correction_z;
```

**限幅说明**：±0.05 rad/s 约 ±2.9°/s，防止错误系数导致过度补偿

### 3.5 飞控在线Bias学习方案

#### 3.5.1 静止学习（推荐启用）

```c
#define ONLINE_BIAS_TAU_S    8.0f    // 时间常数
#define ONLINE_BIAS_LIMIT    0.08f   // 绝对限幅 (rad/s)

// 静止条件
bool is_static = motor_inactive 
    && disarmed 
    && (fabsf(acc_norm - 9.81f) < 0.02f)
    && (gyro_norm < 0.08f);

// 学习更新 (一阶滤波)
float gain = dt / (ONLINE_BIAS_TAU_S + dt);
bias.x += gain * (gyro.x - bias.x);
bias.y += gain * (gyro.y - bias.y);
bias.z += gain * (gyro.z - bias.z);

// 限幅
bias.x = fclip(bias.x, -ONLINE_BIAS_LIMIT, ONLINE_BIAS_LIMIT);
bias.y = fclip(bias.y, -ONLINE_BIAS_LIMIT, ONLINE_BIAS_LIMIT);
bias.z = fclip(bias.z, -ONLINE_BIAS_LIMIT, ONLINE_BIAS_LIMIT);
```

**设计原理**：
- 时间常数8秒，约8秒收敛63%，24秒收敛95%
- 限幅±0.08 rad/s (±4.6°/s)，防止异常值污染
- 只在静止时学习，避免动态时误学习

#### 3.5.2 飞行学习（默认关闭，不推荐用于本工程）

```c
#define FLIGHT_BIAS_ENABLE   0       // 默认关闭
#define FLIGHT_BIAS_TAU_S    60.0f   // 时间常数60秒
#define FLIGHT_BIAS_LIMIT    0.035f  // 限幅更严格

// 条件更严格
bool can_learn = (acc_trust > 0.95f) 
    && (gyro_norm < 0.10f)
    && (fabsf(throttle_delta) < 0.015f)
    && (stable_time > 1000ms);
```

---

## 4. 改进方案设计

### 4.1 总体改进架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        改进后系统架构                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  IMU硬件                                                        │
│      │                                                           │
│      ├──→ imu_preprocess.c: IMU数据预处理 (新增)                 │
│      │       ├── 启动标定 (保留)                                 │
│      │       ├── 温漂补偿 (新增)                                 │
│      │       ├── 在线bias学习 (新增)                             │
│      │       └── Gyro低通滤波 (新增, 可选)                       │
│      │                                                           │
│      ├──→ ekf.c: 四元数EKF姿态估计 (改进)                        │
│      │       ├── 自适应R (改进)                                  │
│      │       ├── Acc Trust门控 (新增)                            │
│      │       ├── Chi2门控 (改进)                                 │
│      │       ├── Joseph形式协方差更新 (改进)                      │
│      │       └── 输出: euler_angle + acc_trust                   │
│      │                                                           │
│      └──→ inertial_nav.c: 惯性导航解算 (改进)                    │
│              ├── 打滑检测增强 (改进)                              │
│              ├── 加速度积分融合 (改进)                            │
│              └── 输出: x, y, relative_yaw, vx_body, vy_body      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 改进1: EKF自适应R + Acc Trust (最高优先级)

#### 4.2.1 修改内容

**文件**: `code/calculate/ekf.c`

**修改点1**: 添加Acc Trust相关宏定义和变量

```c
// ========== 新增: Acc Trust 参数 ==========
#define EKF_R0                  200.0f    // 基础观测噪声 (原10000，大幅降低)
#define EKF_R_ALPHA             30.0f     // 模长偏离放大系数
#define EKF_R_MIN               10.0f     // R下界
#define EKF_R_MAX_NORMAL        5000.0f   // 正常R上限
#define EKF_R_MAX_DYNAMIC       8000.0f   // 动态低trust R上限

#define ACC_TRUST_NORM_SOFT     0.03f     // 模长信任软门限 (g)
#define ACC_TRUST_NORM_HARD     0.18f     // 模长信任硬门限 (g)
#define ACC_TRUST_INNOV_SOFT    0.035f    // 创新信任软门限
#define ACC_TRUST_INNOV_HARD    0.22f     // 创新信任硬门限
#define ACC_TRUST_MIN           0.08f     // 最低信任阈值

#define EKF_CHI2_GATE           9.0f      // Chi2门限 (~3σ)
#define EKF_REJECT_R_INFLATE    50.0f     // Reject后R膨胀倍数

// 新增全局变量
volatile float g_acc_trust = 0.0f;        // 当前acc信任度 (供调试)
volatile uint32_t g_ekf_skip_count = 0;   // 跳过校正计数
volatile uint32_t g_ekf_reject_count[3] = {0}; // 逐轴reject计数
```

**修改点2**: 添加ramp_down函数

```c
/**
 * @brief 斜坡下降函数
 * @param value 输入值
 * @param soft 软门限 (低于此值返回1.0)
 * @param hard 硬门限 (高于此值返回0.0)
 * @return 1.0到0.0之间的线性插值
 */
static float ramp_down(float value, float soft, float hard) {
    if (value <= soft) return 1.0f;
    if (value >= hard) return 0.0f;
    return 1.0f - (value - soft) / (hard - soft);
}
```

**修改点3**: 修改EKF_UpData函数

```c
void EKF_UpData(void) {
    // ... (前面的数据获取、状态预测、协方差预测保持不变) ...
    
    // ========== 新增: 计算Acc Trust ==========
    float acc_norm = sqrtf(imu_data.acc_x * imu_data.acc_x + 
                           imu_data.acc_y * imu_data.acc_y + 
                           imu_data.acc_z * imu_data.acc_z);
    float a_err_g = fabsf(acc_norm - 1.0f);  // 归一化后1g
    
    // 计算创新 (在H矩阵计算之后)
    matrix_t EK = subtract_matrices(&Z, &predicted_gravity);
    float innov_norm = sqrtf(EK.data[0][0] * EK.data[0][0] + 
                             EK.data[1][0] * EK.data[1][0] + 
                             EK.data[2][0] * EK.data[2][0]);
    float innov_xy = sqrtf(EK.data[0][0] * EK.data[0][0] + 
                           EK.data[1][0] * EK.data[1][0]);
    
    // 三维信任
    float trust_norm = ramp_down(a_err_g, ACC_TRUST_NORM_SOFT, ACC_TRUST_NORM_HARD);
    float trust_innov = ramp_down(innov_norm, ACC_TRUST_INNOV_SOFT, ACC_TRUST_INNOV_HARD);
    float trust_xy = ramp_down(innov_xy, 0.030f, 0.20f);
    
    g_acc_trust = trust_norm * trust_innov * trust_xy;
    
    // 信任度过低，跳过校正
    if (g_acc_trust < ACC_TRUST_MIN) {
        g_ekf_skip_count++;
        quaternion_to_euler();
        return;
    }
    
    // ========== 修改: 自适应R ==========
    float r_adapt = EKF_R0 * (1.0f + EKF_R_ALPHA * a_err_g * a_err_g);
    float r_eff = fclip(r_adapt / (g_acc_trust * g_acc_trust), 
                        EKF_R_MIN, EKF_R_MAX_NORMAL);
    
    // 使用自适应R替代固定R
    matrix_t R_adaptive;
    Matrix_Init(&R_adaptive, 3, 3);
    R_adaptive.data[0][0] = r_eff;
    R_adaptive.data[1][1] = r_eff;
    R_adaptive.data[2][2] = r_eff;
    
    // ========== 修改: Chi2门控 ==========
    matrix_t S = add_matrices(&HPHt, &R_adaptive);
    
    // 逐轴检查
    uint8_t reject[3] = {0};
    for (int i = 0; i < 3; i++) {
        float nis = EK.data[i][0] * EK.data[i][0] / S.data[i][i];
        if (nis > EKF_CHI2_GATE) {
            reject[i] = 1;
            R_adaptive.data[i][i] *= EKF_REJECT_R_INFLATE;
            g_ekf_reject_count[i]++;
        }
    }
    
    // 全轴拒绝则跳过
    if (reject[0] && reject[1] && reject[2]) {
        quaternion_to_euler();
        return;
    }
    
    // 重新计算S (如果R被膨胀)
    if (reject[0] || reject[1] || reject[2]) {
        S = add_matrices(&HPHt, &R_adaptive);
    }
    
    // ========== 修改: Joseph形式协方差更新 ==========
    // 计算卡尔曼增益
    matrix_t Kk = multiply_matrices(&PHt, &inv_S);
    
    // 状态更新
    matrix_t temp = multiply_matrices(&Kk, &EK);
    exf_x = add_matrices(&exf_x, &temp);
    normalize_vector(&exf_x);
    
    // Joseph形式: P = (I-KH)P(I-KH)' + KRK'
    matrix_t A = subtract_matrices(&I, &multiply_matrices(&Kk, &H));
    matrix_t AKH = multiply_matrices(&A, &P);
    P = multiply_matrices(&AKH, &Matrix_Transpose(&A));
    matrix_t KR = multiply_matrices(&Kk, &R_adaptive);
    P = add_matrices(&P, &multiply_matrices(&KR, &Matrix_Transpose(&Kk)));
    
    // 协方差对称化 + 限幅
    // ...
    
    quaternion_to_euler();
}
```

#### 4.2.2 参数调优指南

| 参数 | 初始值 | 调优方向 |
|------|--------|---------|
| `EKF_R0` | 200 | 静态漂移大→减小；动态抖动→增大 |
| `EKF_R_ALPHA` | 30 | 加速时姿态不稳→增大；响应慢→减小 |
| `ACC_TRUST_NORM_SOFT` | 0.03 | 过于敏感→增大 |
| `ACC_TRUST_NORM_HARD` | 0.18 | 加速时仍被干扰→减小 |
| `EKF_CHI2_GATE` | 9.0 | 误reject多→增大；异常值漏检→减小 |

### 4.3 改进2: 温度漂移补偿

#### 4.1.1 离线标定流程

**目标**: 获取三轴陀螺仪零偏随温度变化的斜率

**步骤**:

1. **准备环境**
   - 方法A: 温箱（推荐，可控制温度范围）
   - 方法B: 自然环境（开机后温度逐渐上升，覆盖范围有限）

2. **采集数据**
   ```c
   // 在 imu_preprocess.c 中添加温度记录功能
   #define TEMP_DRIFT_WINDOW_SAMPLES  256
   
   typedef struct {
       float temp_sum;
       float gyro_x_sum;
       float gyro_y_sum;
       float gyro_z_sum;
       uint32_t count;
   } TempDriftWindow_t;
   
   // 每次窗口满256个样本，输出一个数据点
   // 格式: timestamp, temperature, gyro_x, gyro_y, gyro_z
   ```

3. **数据处理**
   ```
   // 使用VOFA或串口导出CSV
   // Python脚本拟合:
   import numpy as np
   from scipy import stats
   
   data = np.loadtxt('temp_drift.csv', delimiter=',')
   temp = data[:, 1]
   gyro_x = data[:, 2]
   
   slope_x, intercept_x, r_x, p_x, se_x = stats.linregress(temp, gyro_x)
   print(f"X轴斜率: {slope_x:.8f} rad/s/°C")
   ```

4. **记录结果**
   ```c
   // 在 imu_preprocess.c 中填入拟合结果
   #define IMU_TEMP_COMP_ENABLE        1
   #define IMU_TEMP_COMP_GYRO_X_SLOPE  0.00111208f  // 示例值，需实际标定
   #define IMU_TEMP_COMP_GYRO_Y_SLOPE  -0.00017554f
   #define IMU_TEMP_COMP_GYRO_Z_SLOPE  -0.00004176f
   #define IMU_TEMP_COMP_LIMIT         0.05f        // 补偿限幅 (rad/s)
   ```

#### 4.1.2 在线补偿实现

**文件**: 新增 `code/calculate/imu_preprocess.c` 和 `imu_preprocess.h`

```c
// imu_preprocess.h
#ifndef _IMU_PREPROCESS_H_
#define _IMU_PREPROCESS_H_

#include "zf_common_headfile.h"

// 温漂补偿参数 (需要标定填入)
#define IMU_TEMP_COMP_ENABLE        1
#define IMU_TEMP_COMP_GYRO_X_SLOPE  0.00111208f   // rad/s/°C
#define IMU_TEMP_COMP_GYRO_Y_SLOPE  -0.00017554f
#define IMU_TEMP_COMP_GYRO_Z_SLOPE  -0.00004176f
#define IMU_TEMP_COMP_LIMIT         0.05f

// 在线Bias学习参数
#define IMU_ONLINE_BIAS_ENABLE      1
#define IMU_ONLINE_BIAS_TAU_S       8.0f
#define IMU_ONLINE_BIAS_LIMIT       0.08f

// 数据结构
typedef struct {
    // 温漂补偿
    float bias_temp_degc;           // 标定时的参考温度
    
    // 在线Bias学习
    float online_bias_x;            // 在线学习的零偏
    float online_bias_y;
    float online_bias_z;
    uint32_t static_counter;        // 静止计数器
} IMU_Preprocess_t;

// 函数声明
void IMU_Preprocess_Init(float initial_temp);
void IMU_Preprocess_Update(float *gyro_x, float *gyro_y, float *gyro_z, 
                           float acc_x, float acc_y, float acc_z,
                           float current_temp);
uint8_t IMU_Preprocess_IsStatic(float acc_x, float acc_y, float acc_z,
                                float gyro_x, float gyro_y, float gyro_z);

#endif
```

```c
// imu_preprocess.c
#include "imu_preprocess.h"
#include <math.h>

IMU_Preprocess_t imu_preprocess;

void IMU_Preprocess_Init(float initial_temp) {
    imu_preprocess.bias_temp_degc = initial_temp;
    imu_preprocess.online_bias_x = 0.0f;
    imu_preprocess.online_bias_y = 0.0f;
    imu_preprocess.online_bias_z = 0.0f;
    imu_preprocess.static_counter = 0;
}

uint8_t IMU_Preprocess_IsStatic(float acc_x, float acc_y, float acc_z,
                                float gyro_x, float gyro_y, float gyro_z) {
    float acc_norm = sqrtf(acc_x*acc_x + acc_y*acc_y + acc_z*acc_z);
    float gyro_norm = sqrtf(gyro_x*gyro_x + gyro_y*gyro_y + gyro_z*gyro_z);
    
    // 静止条件
    if (fabsf(acc_norm - 1.0f) > 0.02f) return 0;  // 加速度偏差过大
    if (gyro_norm > 0.08f) return 0;                 // 角速度过大
    
    return 1;
}

void IMU_Preprocess_Update(float *gyro_x, float *gyro_y, float *gyro_z,
                           float acc_x, float acc_y, float acc_z,
                           float current_temp) {
    
    // ========== 1. 温漂补偿 ==========
#if IMU_TEMP_COMP_ENABLE
    {
        float delta_temp = current_temp - imu_preprocess.bias_temp_degc;
        float comp_x = IMU_TEMP_COMP_GYRO_X_SLOPE * delta_temp;
        float comp_y = IMU_TEMP_COMP_GYRO_Y_SLOPE * delta_temp;
        float comp_z = IMU_TEMP_COMP_GYRO_Z_SLOPE * delta_temp;
        
        // 限幅
        comp_x = fclip(comp_x, -IMU_TEMP_COMP_LIMIT, IMU_TEMP_COMP_LIMIT);
        comp_y = fclip(comp_y, -IMU_TEMP_COMP_LIMIT, IMU_TEMP_COMP_LIMIT);
        comp_z = fclip(comp_z, -IMU_TEMP_COMP_LIMIT, IMU_TEMP_COMP_LIMIT);
        
        *gyro_x -= comp_x;
        *gyro_y -= comp_y;
        *gyro_z -= comp_z;
    }
#endif
    
    // ========== 2. 在线Bias学习 ==========
#if IMU_ONLINE_BIAS_ENABLE
    {
        float dt = 0.001f;  // 1ms
        float gain = dt / (IMU_ONLINE_BIAS_TAU_S + dt);
        
        if (IMU_Preprocess_IsStatic(acc_x, acc_y, acc_z, 
                                     *gyro_x, *gyro_y, *gyro_z)) {
            imu_preprocess.static_counter++;
            
            // 连续静止100ms后开始学习
            if (imu_preprocess.static_counter > 100) {
                imu_preprocess.online_bias_x += gain * (*gyro_x - imu_preprocess.online_bias_x);
                imu_preprocess.online_bias_y += gain * (*gyro_y - imu_preprocess.online_bias_y);
                imu_preprocess.online_bias_z += gain * (*gyro_z - imu_preprocess.online_bias_z);
                
                // 限幅
                imu_preprocess.online_bias_x = fclip(imu_preprocess.online_bias_x, 
                                                      -IMU_ONLINE_BIAS_LIMIT, 
                                                      IMU_ONLINE_BIAS_LIMIT);
                imu_preprocess.online_bias_y = fclip(imu_preprocess.online_bias_y, 
                                                      -IMU_ONLINE_BIAS_LIMIT, 
                                                      IMU_ONLINE_BIAS_LIMIT);
                imu_preprocess.online_bias_z = fclip(imu_preprocess.online_bias_z, 
                                                      -IMU_ONLINE_BIAS_LIMIT, 
                                                      IMU_ONLINE_BIAS_LIMIT);
            }
        } else {
            imu_preprocess.static_counter = 0;
        }
        
        // 应用在线学习的零偏
        *gyro_x -= imu_preprocess.online_bias_x;
        *gyro_y -= imu_preprocess.online_bias_y;
        *gyro_z -= imu_preprocess.online_bias_z;
    }
#endif
}
```

### 4.4 改进3: 零点标定增强

#### 4.4.1 修改内容

**文件**: `code/calculate/ekf.c`

```c
// 增强的标定函数
void IMU_Calibrate_All_Gyro_Enhanced(void) {
    const int sample_count = 2000;
    float sum_gx = 0, sum_gy = 0, sum_gz = 0;
    float sum_ax = 0, sum_ay = 0, sum_az = 0;
    float sum_temp = 0;
    int valid_count = 0;
    
    // 静止检测门控
    for (int i = 0; i < sample_count; i++) {
        IMU_GET_GYRO();
        IMU_GET_ACC();
        
        // 检查数据有效性
        if (IMU_GYRO_X == 0 && IMU_GYRO_Y == 0 && IMU_GYRO_Z == 0) continue;
        
        float ax = (float)IMU_ACC_X;
        float ay = (float)IMU_ACC_Y;
        float az = (float)IMU_ACC_Z;
        float acc_norm = sqrtf(ax*ax + ay*ay + az*az);
        
        // 静止门控: 加速度模长接近1g
        if (fabsf(acc_norm - 16384.0f) > 1638.0f) {  // 约±0.1g
            // 不静止，重置计数
            valid_count = 0;
            sum_gx = sum_gy = sum_gz = 0;
            sum_ax = sum_ay = sum_az = 0;
            sum_temp = 0;
            continue;
        }
        
        sum_gx += IMU_GYRO_X;
        sum_gy += IMU_GYRO_Y;
        sum_gz += IMU_GYRO_Z;
        sum_ax += ax;
        sum_ay += ay;
        sum_az += az;
        
        // 读取温度 (如果IMU支持)
        // sum_temp += imu_get_temperature();
        
        valid_count++;
        
        system_delay_us(500);
    }
    
    if (valid_count < 1000) {
        // 标定失败，静止时间不足
        // 可以通过LED或蜂鸣器报警
        return;
    }
    
    // 计算零偏
    gyro_offset_x = sum_gx / valid_count;
    gyro_offset_y = sum_gy / valid_count;
    gyro_offset_z = sum_gz / valid_count;
    acc_offset_x = sum_ax / valid_count;
    acc_offset_y = sum_ay / valid_count;
    acc_offset_z = sum_az / valid_count - 16384.0f;  // Z轴减去1g
    
    // 记录标定时温度
    // imu_preprocess.bias_temp_degc = sum_temp / valid_count;
}
```

### 4.5 改进4: Gyro低通滤波 (可选)

#### 4.5.1 一阶低通实现

```c
// imu_preprocess.c 中添加

#define GYRO_LPF_CUTOFF_HZ  60.0f   // 截止频率
#define GYRO_LPF_SAMPLE_HZ  1000.0f // 采样率

typedef struct {
    float alpha;
    float prev_x, prev_y, prev_z;
    uint8_t initialized;
} GyroLPF_t;

static GyroLPF_t gyro_lpf;

void GyroLPF_Init(void) {
    float rc = 1.0f / (2.0f * 3.14159f * GYRO_LPF_CUTOFF_HZ);
    float dt = 1.0f / GYRO_LPF_SAMPLE_HZ;
    gyro_lpf.alpha = dt / (rc + dt);
    gyro_lpf.prev_x = 0;
    gyro_lpf.prev_y = 0;
    gyro_lpf.prev_z = 0;
    gyro_lpf.initialized = 0;
}

void GyroLPF_Apply(float *x, float *y, float *z) {
    if (!gyro_lpf.initialized) {
        gyro_lpf.prev_x = *x;
        gyro_lpf.prev_y = *y;
        gyro_lpf.prev_z = *z;
        gyro_lpf.initialized = 1;
        return;
    }
    
    gyro_lpf.prev_x += gyro_lpf.alpha * (*x - gyro_lpf.prev_x);
    gyro_lpf.prev_y += gyro_lpf.alpha * (*y - gyro_lpf.prev_y);
    gyro_lpf.prev_z += gyro_lpf.alpha * (*z - gyro_lpf.prev_z);
    
    *x = gyro_lpf.prev_x;
    *y = gyro_lpf.prev_y;
    *z = gyro_lpf.prev_z;
}
```

---

## 5. 分步实施计划

### 5.1 阶段划分

| 阶段 | 内容 | 预计工时 | 优先级 |
|------|------|---------|-------|
| **阶段1** | EKF自适应R + Acc Trust | 2-3天 | ⭐⭐⭐⭐⭐ |
| **阶段2** | 温漂补偿 | 1-2天 + 标定实验 | ⭐⭐⭐⭐ |
| **阶段3** | 在线Bias学习 | 1天 | ⭐⭐⭐⭐ |
| **阶段4** | 零点标定增强 | 0.5天 | ⭐⭐⭐ |
| **阶段5** | Gyro低通滤波 | 0.5天 | ⭐⭐ |

### 5.2 阶段1: EKF自适应R + Acc Trust

#### 5.2.1 前置条件

- [ ] 确认当前EKF可正常工作（能输出正确的欧拉角）
- [ ] 准备VOFA或串口调试工具，能实时观察以下变量：
  - `euler_angle.roll/pitch/yaw`
  - `g_acc_trust`
  - `g_ekf_skip_count`
  - `imu_data.gyro_x/y/z`
  - `imu_data.acc_x/y/z`

#### 5.2.2 实施步骤

1. **备份当前代码**
   ```
   备份 code/calculate/ekf.c 和 ekf.h
   ```

2. **添加新宏定义和变量** (ekf.c)
   ```c
   // 在文件顶部添加
   #define EKF_R0              200.0f
   #define EKF_R_ALPHA         30.0f
   #define EKF_R_MIN           10.0f
   #define EKF_R_MAX_NORMAL    5000.0f
   #define ACC_TRUST_NORM_SOFT 0.03f
   #define ACC_TRUST_NORM_HARD 0.18f
   #define ACC_TRUST_INNOV_SOFT 0.035f
   #define ACC_TRUST_INNOV_HARD 0.22f
   #define ACC_TRUST_MIN       0.08f
   #define EKF_CHI2_GATE       9.0f
   
   volatile float g_acc_trust = 0.0f;
   volatile uint32_t g_ekf_skip_count = 0;
   ```

3. **添加ramp_down函数** (ekf.c)
   ```c
   static float ramp_down(float value, float soft, float hard) {
       if (value <= soft) return 1.0f;
       if (value >= hard) return 0.0f;
       return 1.0f - (value - soft) / (hard - soft);
   }
   ```

4. **修改EKF_UpData函数**
   - 在计算H矩阵之后，计算acc_norm和innovation
   - 计算acc_trust
   - 添加acc_trust门控
   - 使用自适应R替代固定R
   - 添加Chi2门控
   - 使用Joseph形式更新P

5. **添加调试输出** (可选)
   ```c
   // 在wifi_protocol.c或menu.c中添加
   void send_ekf_debug(void) {
       wifi_printf("trust:%.3f,skip:%d\r\n", g_acc_trust, g_ekf_skip_count);
   }
   ```

#### 5.2.3 验证方法

| 测试项 | 操作 | 预期结果 |
|--------|------|---------|
| 静态测试 | 静止放置5分钟 | `g_acc_trust ≈ 1.0`，`g_ekf_skip_count ≈ 0`，航向漂移 < 1° |
| 动态测试 | 快速加减速 | `g_acc_trust` 在加速时降低，姿态保持稳定 |
| 碰撞测试 | 轻轻碰撞机器人 | `g_ekf_skip_count` 增加，姿态快速恢复 |

#### 5.2.4 参数调优

如果测试不通过，按以下顺序调整：

| 问题 | 调整参数 | 调整方向 |
|------|---------|---------|
| 静态漂移仍大 | `EKF_R0` | 减小 (如100) |
| 动态时姿态抖动 | `EKF_R_ALPHA` | 增大 (如50) |
| acc校正过于频繁被拒 | `ACC_TRUST_NORM_HARD` | 增大 (如0.25) |
| 加速时姿态仍被干扰 | `ACC_TRUST_NORM_HARD` | 减小 (如0.12) |

### 5.3 阶段2: 温度漂移补偿

#### 5.3.1 前置条件

- [ ] 阶段1完成并验证通过
- [ ] 准备温度采集环境（温箱或可加热/降温的环境）
- [ ] 确认IMU能读取温度寄存器（IMU660RA/RB/963RA均支持）

#### 5.3.2 实施步骤

1. **创建 imu_preprocess.c/h**
   - 按照4.3节代码创建新文件
   - 添加到工程中

2. **添加温度读取功能**
   ```c
   // 在IMU驱动中添加温度读取
   // IMU660RA: 寄存器 0x01-0x02
   // IMU660RB: 寄存器 0x09-0x0A (TEMP_DATA1, TEMP_DATA0)
   // IMU963RA: 寄存器 0x41-0x42
   
   float imu_get_temperature(void) {
       // 读取温度寄存器并转换
       // 具体公式参考数据手册
   }
   ```

3. **添加温度记录功能**
   ```c
   // 在VOFA或串口输出中添加温度和gyro数据
   void send_temp_drift_data(void) {
       float temp = imu_get_temperature();
       wifi_printf("%.2f,%.6f,%.6f,%.6f\r\n", 
                   temp, 
                   imu_data.gyro_x - gyro_offset_x,
                   imu_data.gyro_y - gyro_offset_y,
                   imu_data.gyro_z - gyro_offset_z);
   }
   ```

4. **执行温度标定实验**
   ```
   a. 将机器人放入温箱（或自然环境）
   b. 设置温度范围: 15°C ~ 45°C（或更大范围）
   c. 每隔2°C保持静止5分钟
   d. 记录VOFA输出的CSV数据
   e. 使用Python脚本拟合斜率
   ```

5. **填入拟合结果**
   ```c
   // 在 imu_preprocess.h 中填入
   #define IMU_TEMP_COMP_GYRO_X_SLOPE  0.00123456f  // 实际拟合值
   #define IMU_TEMP_COMP_GYRO_Y_SLOPE  -0.00023456f
   #define IMU_TEMP_COMP_GYRO_Z_SLOPE  -0.00004567f
   ```

6. **集成到EKF数据流**
   ```c
   // 在 imu_get_values() 中调用
   void imu_get_values(void) {
       // ... 原有代码 ...
       
       // 新增: 温漂补偿 + 在线学习
       float temp = imu_get_temperature();
       IMU_Preprocess_Update(&imu_data.gyro_x, &imu_data.gyro_y, &imu_data.gyro_z,
                            imu_data.acc_x, imu_data.acc_y, imu_data.acc_z, temp);
   }
   ```

#### 5.3.3 验证方法

| 测试项 | 操作 | 预期结果 |
|--------|------|---------|
| 温度循环测试 | 15°C→45°C→15°C循环 | 航向漂移 < 2°/全程 |
| 快速温变测试 | 开机后立即运行 | 温漂补偿生效，漂移减小 |

### 5.4 阶段3: 在线Bias学习

#### 5.4.1 前置条件

- [ ] 阶段2完成
- [ ] imu_preprocess.c 已包含在线学习代码

#### 5.4.2 实施步骤

1. **确认在线学习参数**
   ```c
   #define IMU_ONLINE_BIAS_ENABLE  1
   #define IMU_ONLINE_BIAS_TAU_S   8.0f
   #define IMU_ONLINE_BIAS_LIMIT   0.08f
   ```

2. **添加调试输出**
   ```c
   void send_online_bias_debug(void) {
       wifi_printf("bias_x:%.6f,bias_y:%.6f,bias_z:%.6f\r\n",
                   imu_preprocess.online_bias_x,
                   imu_preprocess.online_bias_y,
                   imu_preprocess.online_bias_z);
   }
   ```

3. **集成到主循环**
   - 确保 `IMU_Preprocess_Update()` 在每个IMU采样周期被调用
   - 确保静态检测条件正确

#### 5.4.3 验证方法

| 测试项 | 操作 | 预期结果 |
|--------|------|---------|
| 静止学习测试 | 静止放置30秒 | `online_bias` 逐渐收敛到稳定值 |
| 长时间运行测试 | 运行10分钟后重新静止 | `online_bias` 自动修正漂移 |

### 5.5 阶段4: 零点标定增强

#### 5.5.1 实施步骤

1. **替换 IMU_Calibrate_All_Gyro()**
   - 使用4.4.1节的增强版本
   - 添加静止检测门控
   - 添加标定失败处理

2. **添加标定状态反馈**
   ```c
   volatile uint8_t g_imu_cal_status = 0;  // 0:未标定, 1:成功, 2:失败
   
   void IMU_Calibrate_All_Gyro_Enhanced(void) {
       // ...
       if (valid_count < 1000) {
           g_imu_cal_status = 2;  // 失败
           return;
       }
       g_imu_cal_status = 1;  // 成功
   }
   ```

### 5.6 阶段5: Gyro低通滤波 (可选)

#### 5.6.1 实施步骤

1. **添加GyroLPF代码** (按照4.5.1节)

2. **集成到数据流**
   ```c
   void imu_get_values(void) {
       // ... 原有代码 ...
       
       // 温漂补偿和在线学习
       IMU_Preprocess_Update(...);
       
       // 新增: Gyro低通滤波
       GyroLPF_Apply(&imu_data.gyro_x, &imu_data.gyro_y, &imu_data.gyro_z);
   }
   ```

3. **参数调优**
   - 初始截止频率: 60Hz
   - 如果EKF响应变慢，提高截止频率
   - 如果噪声仍大，降低截止频率

---

## 6. 验收标准与测试方法

### 6.1 静态测试

**目的**: 验证EKF姿态估计的稳定性

**方法**:
1. 将机器人静止放置在水平面上
2. 记录5分钟内的欧拉角变化
3. 观察 `g_acc_trust` 和 `g_ekf_skip_count`

**验收标准**:
| 指标 | 标准 |
|------|------|
| Roll漂移 | < 0.5°/5min |
| Pitch漂移 | < 0.5°/5min |
| Yaw漂移 | < 1°/5min |
| `g_acc_trust` | > 0.9 (静止时) |
| `g_ekf_skip_count` | < 10 (5分钟内) |

### 6.2 动态测试

**目的**: 验证Acc Trust门控在动态时的保护效果

**方法**:
1. 让机器人正常行驶
2. 执行急加速/急减速
3. 观察姿态是否稳定

**验收标准**:
| 指标 | 标准 |
|------|------|
| 加速时姿态抖动 | 无明显抖动 |
| `g_acc_trust` | 加速时下降，匀速时恢复 |
| 航向跟踪 | 弯道行驶后航向误差 < 5° |

### 6.3 温度测试

**目的**: 验证温漂补偿效果

**方法**:
1. 从室温开机运行
2. 记录10分钟内的航向变化
3. 对比改进前后的漂移量

**验收标准**:
| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 10min航向漂移 | > 5° | < 2° |

### 6.4 长时间运行测试

**目的**: 验证在线Bias学习效果

**方法**:
1. 连续运行30分钟
2. 定期记录航向误差
3. 观察 `online_bias` 变化趋势

**验收标准**:
| 指标 | 标准 |
|------|------|
| 30min累计漂移 | < 5° |
| `online_bias` | 逐渐收敛到稳定值 |

---

## 附录A: 代码修改清单

| 文件 | 修改类型 | 修改内容 |
|------|---------|---------|
| `code/calculate/ekf.c` | 修改 | 自适应R、Acc Trust、Chi2门控、Joseph形式更新 |
| `code/calculate/ekf.h` | 修改 | 新增调试变量声明 |
| `code/calculate/imu_preprocess.c` | 新增 | 温漂补偿、在线Bias学习 |
| `code/calculate/imu_preprocess.h` | 新增 | 数据结构和函数声明 |
| `code/tools/wifi_protocol.c` | 修改 | 新增调试数据输出 |

## 附录B: 参考文献

1. 飞控IMU滤波技术文档: `imu高级内容/imu_filter_technology_report.md`
2. 飞控多传感器融合文档: `imu高级内容/imu_optical_flow_ekf_fusion.md`
3. 当前惯导实现: `code/navigation/inertial_nav.c`
4. 当前EKF实现: `code/calculate/ekf.c`

---

> **文档结束**  
> 如有疑问，请参考飞控文档中的详细数学推导和实现细节。
