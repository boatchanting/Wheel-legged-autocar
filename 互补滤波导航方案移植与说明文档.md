# 互补滤波导航方案移植与说明文档（完整版）

本文档基于“双轮足并联腿机器人”的硬件架构，提炼了 GPS 与轮速计/IMU 的互补滤波融合方案的核心逻辑，供新工程移植参考。文档内容贯穿了“底层数据获取 -> 坐标转换 -> 互补滤波融合 -> 上层纯追踪控制 -> 上位机遥测验证”的完整数据流。

> [!IMPORTANT]
> **移植定制要求：**
> 1. 新工程中已废弃“GPS 杆臂补偿”，本文档中相关逻辑已移除。
> 2. “打滑检测”动态改变权重的逻辑，在新工程中受宏开关控制（默认关闭，即采用固定融合权重）。
> 3. 任何坐标系的移植请务必小心核对正负号，底盘与 GPS 坐标系的定义在不同工程中极易出错。

---

## 1. 物理架构与底盘基础认知

目标机器人是**“对称五连杆并联腿双轮足式自平衡机器人”**：
- **纵向速度解耦**：车身向前加速不直接改变车轮转速，而是通过腿部舵机（前腿伸长、后腿收缩）改变重心使车身前倾，从而由高频直立平衡环（1ms）驱动轮子向前加速，即“追重心加速”。
- **横向转向解耦**：整车无专门的转向舵机，偏航转向（Yaw）完全依靠底盘左右两轮无刷电机的差速来实现。
- **底层控制仲裁**：在最终下发 PWM 混控时，必须**最高优先级保证直立平衡输出**（`gyro_loop_out`），其次才是转向与速度前馈。

---

## 2. 坐标系定义与转换规则 (易错点)

本导航方案涉及三个核心坐标系：

### 2.1 GPS 地理坐标系 (高斯投影)
- 将经纬度通过 WGS84 高斯投影算法转化为局部笛卡尔坐标（米）。
- 增量定义：`delta_E`（向东为正），`delta_N`（向北为正）。

### 2.2 惯导与赛道坐标系 (INS Frame)
惯导推算与路径规划（Pure Pursuit）工作在**“赛道基准坐标系”**下：
- **原点**：由发车时手动锁定（此时 `fuse_x = 0, fuse_y = 0`）。
- **航向 (Yaw)**：手动锁定时车头朝向为 0 度，**顺时针为正**。
- **X 轴**：**车身后方为正**（即车辆向前移动时，X 坐标向负方向增大）。
- **Y 轴**：**车身右侧为正**。

### 2.3 车身坐标系 (Body Frame)
- 纵向速度 `vx_body`：**向前为正**。
- 横向速度 `vy_body`：向左侧滑为正。
- 偏航角速度 `gyro_z`：依据具体 IMU 决定，但积分后的 `yaw` 必须匹配赛道坐标系的**顺时针为正**。

### 2.4 从地理坐标转到赛道坐标（核心矩阵）
发车时记录绝对航向 `g_track_base_yaw`（正北为0，顺时针为正）。
代码实现于 `gnss_transform.c`，获取东向(`delta_E`)和北向(`delta_N`)位移后，执行旋转：
```c
float rad = g_track_base_yaw * DEG_TO_RAD;
// X 向后为正，Y 向右为正
float raw_local_x = -(delta_E * sinf(rad) + delta_N * cosf(rad));
float raw_local_y =   delta_E * cosf(rad) - delta_N * sinf(rad);
```

---

## 3. 互补滤波核心算法与数据流

采用**“基于 Offset 的双频互补滤波架构”**：
- **惯导主导 (100Hz)**：保证短时间内的轨迹绝对平滑，解决纯 GPS (10Hz) 带来的锯齿与抽搐。
- **GPS 纠偏 (10Hz)**：通过弹性更新 Offset 来消除惯导的长期累积漂移。

### 3.1 核心数据结构 (`FusionState_t`)
```c
typedef struct {
    float ins_x;      // 惯导内部独立积分 X (mm)
    float ins_y;      // 惯导内部独立积分 Y (mm)
    float ins_yaw;    // 惯导内部独立积分 Yaw (度)

    float offset_x;   // 零点偏移量 X (mm)，由 GPS 弹性修正
    float offset_y;   // 零点偏移量 Y (mm)

    float fuse_x;     // 输出给上层的平滑 X (mm)
    float fuse_y;     // 输出给上层的平滑 Y (mm)
    float fuse_yaw;   // 输出给上层的平滑 Yaw (度)
    
    float k_pos;      // 当前生效的互补滤波权重
} FusionState_t;
```

### 3.2 100Hz 惯导推算链路
执行于 `Fusion_Ins_Update()` (由 10ms 周期中断调用)：
```c
float yaw_rad = g_fuse_state.fuse_yaw * DEG_TO_RAD;

// 车身速度转世界速度 (适配 X向后为正, Y向右为正)
float vx_world = -vx_body * cosf(yaw_rad) - vy_body * sinf(yaw_rad);
float vy_world = -vx_body * sinf(yaw_rad) + vy_body * cosf(yaw_rad);

// INS 独立积分 (dT=0.01s)
g_fuse_state.ins_x += vx_world * 0.01f;
g_fuse_state.ins_y += vy_world * 0.01f;
g_fuse_state.ins_yaw += imu_data.gyro_z * RAD_TO_DEG * 0.01f;

// 实时加上 offset，输出融合坐标
g_fuse_state.fuse_x = g_fuse_state.ins_x + g_fuse_state.offset_x;
g_fuse_state.fuse_y = g_fuse_state.ins_y + g_fuse_state.offset_y;
g_fuse_state.fuse_yaw = g_fuse_state.ins_yaw; // 单天线不修正 yaw
```

### 3.3 10Hz GPS 纠偏与权重调度
执行于 `Fusion_Gps_Correct()` (当串口成功解析 GPS 帧后)：

```c
// 1. 地理位移转赛道本地位移 (逻辑见 2.4)
float rad = g_track_base_yaw * DEG_TO_RAD;
float ground_x = -(delta_E * sinf(rad) + delta_N * cosf(rad));
float ground_y =   delta_E * cosf(rad) - delta_N * sinf(rad);

// 2. GPS 硬件延时前馈补偿
float vx_mps = vx_body * 0.001f;
ground_x = ground_x - vx_mps * GPS_DELAY_SEC * cosf(g_fuse_state.fuse_yaw * DEG_TO_RAD);
ground_y = ground_y - vx_mps * GPS_DELAY_SEC * sinf(g_fuse_state.fuse_yaw * DEG_TO_RAD);

float ground_x_mm = ground_x * 1000.0f;
float ground_y_mm = ground_y * 1000.0f;

// 3. 跃变剔除保护 (创新度滤波)
// 判断依据：GPS投射坐标与当前融合坐标的距离
float dx_gps = ground_x_mm - g_fuse_state.fuse_x;
float dy_gps = ground_y_mm - g_fuse_state.fuse_y;
float delta_gps = sqrtf(dx_gps * dx_gps + dy_gps * dy_gps);

if (delta_gps > 1500.0f) { // 突变超过 1.5m 丢弃，进入超时保护逻辑
    return;
}

// 4. K_pos 战术级调度
float K_pos = 0.02f; // 默认每次拉扯 2%

if (gnss.satellite_used < 10) K_pos = 0.0f;
else if (gnss.satellite_used < 15) K_pos = 0.01f;

// 零速挂起 (ZUPT)：停车时 GPS 散布容易导致底盘乱动
if (fabsf(wheel_speed) < 10.0f && fabsf(gyro_z_deg) < 2.0f) K_pos = 0.0f;

// 特殊元素屏蔽 (雷区/桥等要求高精度的路段完全信任惯导)
if (current_element == NAV_POINT_CIRCLE || current_element == NAV_POINT_BRIDGE) K_pos = 0.0f;

// [移植改动] 打滑权重转移通过宏开关控制
#if ENABLE_SLIP_WEIGHT_SHIFT
if (inertial_nav.slip_flag == 1) K_pos = 0.10f; // 打滑时惯导失真，放大 GPS 权重
#endif

// 5. 弹性更新 Offset (不直接覆盖坐标)
g_fuse_state.offset_x += K_pos * (ground_x_mm - g_fuse_state.fuse_x);
g_fuse_state.offset_y += K_pos * (ground_y_mm - g_fuse_state.fuse_y);
```

---

## 4. 上层控制消费端：纯追踪 (Pure Pursuit)

融合后的坐标在 `plan1_gnss.c` 中被消费。核心原则：**只往前看，绝不回头**。

1. **状态获取**：直接调用接口获取平滑后的 `fuse_x`，`fuse_y` 和 `fuse_yaw`。
2. **前瞻点过滤**：遍历目标点集合，剔除距离自身过近（小于 `GPS_NAV_LOOKAHEAD_DIST`）或已经在车身后方（通过向量点乘判定 `is_passed`）的点。
3. **计算航向偏差**：
   ```c
   target_bearing = GpsCalcBearingDegFromNorth(cx, cy, target_x, target_y);
   raw_err_degree = GpsNormalizeAngle(target_bearing - current_heading);
   ```
4. **输出限幅与降速**：
   - 目标偏角 `err_degree` 被限幅在 `±35.0f`。
   - 弯道自动降速公式：`speed_factor = 1.0f - (abs_err / 40.0f)`，限底 `0.40f`。

---

## 5. 联调验证与配套上位机

### 5.1 上位机工具使用
- 脚本位置：`tools/webview_gps_marker/gps_marker_host.py`
- 功能：基于 TCP 在 `127.0.0.1:8086` 监听设备帧。可视化轨迹，下发“清空轨迹(手动锁定)”和“发车”命令。
- 若需离线开发，可配合 `mock_device.py` 模拟下发含有 GPS 噪声和随机打滑标志的数据包，以验证滤波算法的鲁棒性。

### 5.2 核心验证依据：三线合一
为了验证互补滤波的效果，下位机 `wifi_protocol.c` 中打包发送了三组坐标（单位: mm）：
1. **纯惯导线 (ins)**：`ins_x`, `ins_y` —— 极其平滑，但长期运行会走偏。
2. **GPS 地面真值 (gps)**：`ground_x*1000`, `ground_y*1000` —— 总体走向绝对正确，但存在局部锯齿或跳变。
3. **融合输出 (fusion)**：`fuse_x`, `fuse_y` —— 期望效果是 **和 ins 线一样平滑，但整体走向紧贴 gps 线**。

> [!TIP]
> **移植后的调参步骤**：
> 1. 上电等待 GPS 卫星收敛（>= 15 颗）。
> 2. 摆正车身，点击上位机“清空轨迹”以锁定原点 `fuse_x=0, fuse_y=0`。
> 3. 直推车身（不启动电机），观察 `fuse_x` 是否向负方向增大，`fuse_y` 基本保持在 0，否则说明旋转矩阵正负号存在问题。
> 4. 让车跑一圈，如果车偏离路线很久才回来，调大 `K_pos`；如果车身发生抽搐抖动，调小 `K_pos`。
