# 互补滤波上位机与模拟测试脚本配套使用说明

## 概述

本项目包含两个核心工具，配合使用可完成互补滤波算法的离线调试和验证：

| 工具 | 文件 | 角色 |
|------|------|------|
| 上位机 | `tools/webview_gps_marker/gps_marker_host.py` | TCP 服务端，接收数据、可视化轨迹、打点导出 |
| 模拟脚本 | `tools/webview_gps_marker/mock_device.py` | TCP 客户端，模拟下位机发送融合数据帧 |

两者通过 TCP `127.0.0.1:8086` 通信，帧协议与真实下位机完全一致。

---

## 快速启动

### 1. 启动上位机（先启动）

```bash
python tools/webview_gps_marker/gps_marker_host.py
```

- 自动打开 WebView 窗口显示轨迹
- 后台监听 `127.0.0.1:8086`，等待下位机连接

### 2. 启动模拟脚本（后启动）

```bash
python tools/webview_gps_marker/mock_device.py
```

启动后需选择两项参数：

```
请选择发车角获取模式：
1. 静态多帧磁力计滤波 (发车前2秒) - Mode 1
2. 动态轨迹最小二乘拟合 (发车后5秒) - Mode 2

请选择模拟轨迹的半径尺寸：
1. 3米 (3000mm) - 默认
2. 5米 (5000mm)
3. 10米 (10000mm)
```

连接成功后，上位机即可看到实时轨迹。

---

## 两种发车角模式

### Mode 1：静态磁力计滤波

- 车辆前 2 秒静止，采集磁力计数据做均值滤波
- 2 秒后计算出 `track_base_yaw`，随即开始行驶
- 适合模拟"停车后原地初始化"场景

### Mode 2：动态最小二乘拟合

- 车辆立即行驶，前 50 个 GPS 周期（5 秒）采集 GPS + INS 数据
- 用最小二乘法拟合两组轨迹的旋转偏差
- 适合模拟"边走边初始化"场景

---

## 模拟的异常场景

脚本内置以下异常注入，用于验证滤波器鲁棒性：

| 场景 | 触发条件 | 效果 |
|------|----------|------|
| GPS 随机跳变 | 每次 GPS 更新 2% 概率 | 跳变幅度 ±2m，应被跳变检测拒绝 |
| 长时间漂移 | T=10s ~ 13s | GPS 坐标偏移 +3m，模拟持续漂移 |
| ZUPT 零速修正 | T=15s ~ 18s | 车辆停止，INS 冻结，融合屏蔽 |
| 卫星数随机跳变 | 每 1~2 秒随机一次 | 范围 5~20，影响 GPS 噪声幅度 |

### 卫星数与 GPS 噪声的关系

| 卫星数 | GPS 噪声范围 | 倍率 |
|--------|-------------|------|
| ≥ 15 | ±100mm | 1x（基准） |
| 10 ~ 14 | ±200mm | 2x |
| < 10 | ±400mm | 4x |

---

## 帧发送频率

| 项目 | 频率 |
|------|------|
| 内部数据生成与运算 | 100Hz（10ms 一帧） |
| 实际发送给上位机 | 20fps（每 5 帧发 1 帧） |
| GPS 更新周期 | 10Hz（每 10 帧更新一次） |

---

## 日志功能

每次运行 `mock_device.py`，会在脚本同目录下自动生成 `mock_log.csv`，覆盖上一次的日志。

日志字段：

```
loop, time_s, ins_x, ins_y, ground_x, ground_y, fuse_x, fuse_y,
offset_x, offset_y, k_pos, jump_count, zupt, special, frame_hex
```

- `frame_hex`：完整帧的十六进制，可用于协议级回放或排查
- 其余字段为解码后的关键状态值，便于 Excel/Python 做离线分析

---

## 帧协议格式

```
[0x5A] [0xA5] [CMD] [LEN] [PAYLOAD...] [CHECKSUM] [0xED]
```

| 字段 | 说明 |
|------|------|
| `0x5A 0xA5` | 帧头 |
| `CMD=0x01` | 遥测数据帧 |
| `LEN` | Payload 长度（字节） |
| `CHECKSUM` | 从帧头到 Payload 末尾的字节累加和 & 0xFF |
| `0xED` | 帧尾 |

Payload 使用 V5 格式（139 字节），结构体格式：

```python
"<IffffHBBBBBBHHHHHHddbbffBfBfffBBffBBffffffffffBBB"
```

包含 INS 坐标、GPS 投影坐标、融合输出、滤波器状态等全部调试字段。

---

## 典型调试流程

1. 启动上位机，确认监听端口就绪
2. 启动模拟脚本，选择 Mode 2 + 半径 3m
3. 观察上位机轨迹：
   - 发车角计算完成前，轨迹应为 INS 原始漂移
   - 发车角计算完成后，轨迹应切换到融合输出
4. 观察 T=10~13s 漂移区：融合轨迹应缓慢跟随 GPS 偏移
5. 观察 T=15~18s ZUPT：轨迹应完全冻结
6. 观察随机 GPS 跳变：上位机应拒绝跳变，轨迹不突变
7. 观察卫星数变化：低卫星数时 GPS 噪声增大，融合轨迹抖动加剧
8. Ctrl+C 停止，检查 `mock_log.csv` 做离线分析
