## 调车

可以针对科目一，科目二，科目三启用不同的算法和参数实现？用config控制，例如科一不需要状态机，那状态机就是占用算力的，可以完全不要

记录每个科目的调车记录为日志，记录速度计时，迭代过程
优化科目一参数，逐渐测试自动驾驶极限速度

优化科目二参数，逐渐测试自动驾驶极限速度

最好早上或者下午在操场进行科一科二的测试，把速度提上去

## 其他代码 
3车963ra的替换√

WIFI上位机回传指令到下位机的实现 同样这样的话可以实现发车，最好使用自定义上位机实现，逐飞的那个其实直接调用一下就可以发车了，这个可以学一下视觉组的vofa的下位机代码√

WIFI或者flash实现的小车日志系统，支持总开关，便于调车 就比如之前，那个因为驱动问题所导致的没有发现，如果看日志系统的话，可能就能发现它的整个，死机的过程中，其他参数都是动的，而速度参数一点都不动

遥控器线控转向的实现 就是在小速度下提高转向灵敏度，大速度降低转向灵敏度，修改sbus

Bridge控制参数的整定，现在的实现原理是主动rolling环加惯导距离，参数是只留了一个，可以选择留一个参数层，然后把三车的参数调整调整，譬如参数呀，距离呀，现在距离留的量很大

颠簸路段的参数的整定，同上，其实现原理是给一个恒定的速度目标值显期往过去，然后，通过pitch和期望速度以及实际速度参数去检测电机有没有堵转，如果堵转，那就向后退一点点，然后跳跃

三级跳参数的整定，这个可以先往后放放，这个参数应该难整定，可以等加视觉后，视觉已经搞好，等待一下步融入车机做参数整定

惯导方案中，遇到科目打点，可以科目开始和结束做直线连接，理论上比科目开始加开始角准

发车之后关闭屏幕计算和渲染，可以用遥控或者wifi控停车而不是关使能

汇总科目一 科目二的所有参数，撰写原理以及说明，便于调车
【优化点】科目一，科目二的时候有些功能显然是不需要的，看看要不要编译的时候通过宏定义给去掉，现在都留着也无妨
科目一优化的东西：惯导条件下，去除了任务点搜索code/navigation/nav_replay.c...详情见[小车系统配置](../../code/config/sys_options.h)


## 仓库管理
iar工程文件的同步，01核导入库的隔离

仓库根据tag自动生成两个tag中间的更新日志

考虑将库文件和工程文件同样自动备份与更新，打包，下载即可使用

## 算法
使用eskf算法融合GPS和惯导

将姿态估计的ekf优化为eskf

## 视觉方面 
数据集收集 根据已有的这些控制指令，调用直接在sbus里面用就可以了，将视觉回传的代码烧录到一核，然后再联合调用控制录制整个过程的完整视频，每个项目多来几个视频，三级跳的话数数据收集可以用单级跳命令，然后人开车，上面所有收集的数据集里面都要来一点斜对着赛道的，正对着赛道的√

视觉指令的架构和视觉融合惯导 这个暂时还没想好√

视觉那里的库采样改成188*120，然后压缩为96*60√

|算法|py|c电脑|c车|
|-------|-------|-------|-------|
|逆透视打表算法|√|||
|pvc视觉检测 |√|||
|颠簸路段视觉检测 |√|||
|单边桥视觉检测 |√|||
|操场道线视觉检测 |√|||

---

## 未完成任务的 AI 工作 Prompt（可直接复制使用）

> 说明：以下仅覆盖文档中未标注“√”的任务；每条都按“目标 + 约束 + 交付物 + 验收标准”组织，便于直接交给 AI 执行。

### 1) 科目一/二/三按 config 启用不同算法与参数
**Prompt：**
请为车队项目设计并实现“按科目切换算法与参数”的配置系统。要求：
1. 使用统一 config（建议 `sys_options.h` + 独立 subject 配置头文件）控制科目一、科目二、科目三的功能开关。
2. 科目一可关闭状态机等非必要模块，减少算力占用；科目二、科目三保留必要模块。
3. 给出模块级开关清单（例如：状态机、视觉融合、日志、屏幕渲染等）和默认值。
4. 提供最小改动的接入方案，不破坏现有流程。
5. 输出：改动文件列表、关键代码、配置说明、回滚方式。
6. 验收：编译通过；不同科目切换后，日志可打印当前启用模块列表。

### 2) 记录每个科目的调车日志（速度计时、迭代过程）
**Prompt：**
请实现“按科目记录调车日志系统”。要求：
1. 日志包含：科目类型、日期时间、参数版本、目标速度、实测速度、单圈/单段计时、迭代备注。
2. 支持总开关，关闭时几乎无性能开销。
3. 支持串口/WIFI/flash 中至少一种稳定落盘或回传方式。
4. 给出日志数据结构、字段解释、示例输出。
5. 提供一个简单脚本/方法，将日志汇总成“速度提升趋势表”。
6. 验收：能连续记录 10 次以上测试并正确导出。

### 3) 优化科目一参数并测试极限速度
**Prompt：**
请制定并执行“科目一参数优化到极限速度”的实验方案。要求：
1. 先给出可调参数分组（转向、速度环、姿态、保护阈值等）和敏感度排序。
2. 采用分阶段提速（例如每次 +0.2m/s 或固定 PWM 步进），每阶段设安全回退条件。
3. 自动生成每轮测试记录模板（参数、速度、稳定性、越界次数、是否完赛）。
4. 输出推荐参数区间，而不是单一参数点。
5. 验收：给出“当前稳定最高速度”“风险最高参数项”“下一步建议”。

### 4) 优化科目二参数并测试极限速度
**Prompt：**
请制定“科目二参数优化与极限速度测试”方案，重点关注连续弯道与任务切换稳定性。要求：
1. 参数分层：基础控制层、任务策略层、保护层。
2. 定义稳定性指标：摆动幅度、冲出边线次数、任务完成率、平均速度。
3. 采用正交或网格化小规模搜索，避免盲目人工调参。
4. 输出：最优参数组合 Top-N、失败样本复盘、推荐上线参数。
5. 验收：在既定测试路线连续通过率达到目标阈值（请先给出建议阈值）。

### 5) 组织操场时段测试计划（科一/科二提速）
**Prompt：**
请输出一份“操场早晚测试执行计划”。要求：
1. 分早上/下午两个时段，列出测试目标、人员分工、设备清单。
2. 每轮测试包含：预检、热身、提速、回归、收尾五步。
3. 给出风险项与中止条件（天气、场地占用、温度、通信异常等）。
4. 输出可打印 checklist 和 1 页测试记录表模板。

### 6) WIFI 上位机回传指令到下位机（支持发车）
**Prompt：**
请实现“自定义 WIFI 上位机 -> 下位机指令链路”，并支持发车控制。要求：
1. 参考现有逐飞/vofa 下位机思路，抽象统一指令协议（帧头、命令字、长度、校验）。
2. 最小命令集：发车、停车、急停、参数下发、状态查询。
3. 增加通信超时与重发机制，防止误触发。
4. 给出上位机最小 Demo（可用 Python 或 C#）和下位机解析代码。
5. 验收：连续 100 次发车/停车指令正确率统计。

### 7) WIFI/flash 日志系统（可总开关）
**Prompt：**
请实现“WIFI/flash 双通道可选日志系统”，用于定位死机与驱动异常。要求：
1. 支持编译期和运行期开关，默认低开销。
2. 关键字段必须包含：速度相关参数、驱动状态、任务状态、异常码、时间戳。
3. 提供循环缓冲区方案，避免写满导致阻塞。
4. 给出死机前后日志保留策略与故障复盘流程。
5. 验收：模拟速度参数卡死场景，日志可明显定位异常。

### 8) 遥控器线控转向（低速灵敏/高速抑制）
**Prompt：**
请基于 SBUS 实现“速度相关转向增益”功能。要求：
1. 低速区提高转向灵敏度，高速区降低转向灵敏度；使用分段函数或平滑插值。
2. 给出参数：速度分界点、最大/最小转向倍率、平滑时间常数。
3. 增加防抖与极值保护，避免转向突变。
4. 输出：核心代码、参数建议、实车调参步骤。
5. 验收：不同速度段摇杆同幅输入时，车辆响应符合预期且无突变。

### 9) Bridge 控制参数整定
**Prompt：**
请对 Bridge 控制策略做参数整定与结构梳理。要求：
1. 梳理现有“rolling 环 + 惯导距离”实现和唯一参数的作用链路。
2. 评估是否保留单参数层，或拆分为“距离参数 + 姿态参数 + 速度参数”。
3. 结合三车差异给出参数迁移方法（基准车 -> 目标车）。
4. 输出：整定步骤、推荐默认值、异常表现到参数项的映射表。
5. 验收：三车都能在统一方法下快速收敛到可用参数。

### 10) 颠簸路段参数整定
**Prompt：**
请针对颠簸路段控制策略进行参数整定。要求：
1. 基于“恒定目标速度 + pitch + 期望/实际速度判堵转 + 后退跳跃”的现有逻辑，先画状态流程图。
2. 定义关键阈值：堵转判定阈值、后退距离、跳跃触发窗口、超时退出条件。
3. 给出参数扫描顺序与安全边界，避免频繁误判。
4. 输出：整定前后对比数据（通过率、平均耗时、卡死次数）。
5. 验收：连续多次通过率提升且卡死率下降。

### 11) 三级跳参数整定（后置任务）
**Prompt：**
请制定“三级跳参数整定预案（视觉融合后执行）”。要求：
1. 先输出当前阶段不执行的理由与前置依赖（视觉模块接口、车机融合点）。
2. 给出未来可直接执行的整定 SOP：数据采集、初值设定、逐级调参、验收。
3. 提供占位参数表和待观测指标，便于后续直接填数。
4. 输出一个“触发执行条件清单”（满足哪些条件才正式开始整定）。

### 12) 惯导打点方案优化（科目起止点直线连接）
**Prompt：**
请设计“科目起点-终点直线连接”的惯导打点方案，并与“起始角方案”对比。要求：
1. 从几何与误差传播角度解释两种方案差异。
2. 给出数据结构和计算流程（采点、拟合、更新控制目标）。
3. 输出 A/B 对比实验设计：偏航误差、轨迹偏移、完赛稳定性。
4. 验收：在至少 N 组测试中，直线连接方案指标优于或不劣于旧方案（请给 N 建议值）。

### 13) 发车后关闭屏幕计算与渲染
**Prompt：**
请实现“发车后自动关闭屏幕计算与渲染，停车后可恢复”的机制。要求：
1. 保留遥控/WIFI 停车能力，不依赖关使能停车。
2. 明确哪些显示相关任务可停、哪些必须保留（如关键状态上报）。
3. 增加状态机与异常回退（比如停车失败时强制恢复显示线程）。
4. 输出性能对比：CPU 占用、帧率、控制环抖动前后差异。
5. 验收：发车后性能改善可量化，且停车控制稳定。

### 14) 汇总科目一/科目二参数并撰写原理说明
**Prompt：**
请产出“科目一/科目二参数手册”。要求：
1. 参数按模块分类：导航、控制、保护、任务策略、视觉相关。
2. 每个参数包含：含义、单位、推荐范围、风险、调大/调小的现象。
3. 增加“典型问题 -> 参数排查路径”速查表。
4. 输出 Markdown 文档，可直接放入 `docs/`。
5. 验收：新同学按文档可独立完成一次基础调参。

### 15) 编译期宏裁剪科目一/科目二非必要功能
**Prompt：**
请为科目一/科目二实现“编译期宏裁剪不必要功能”的方案。要求：
1. 识别非必要模块并分级（可删/建议删/保留）。
2. 使用宏开关裁剪代码路径，避免运行时分支开销。
3. 给出二进制体积、RAM 占用、CPU 占用对比。
4. 输出：宏定义表、默认组合、兼容性注意事项。
5. 验收：裁剪后功能正确、性能或资源占用有明确收益。

### 16) IAR 工程文件同步与 01 核导入库隔离
**Prompt：**
请制定并实现“IAR 工程同步 + 01 核导入库隔离”方案。要求：
1. 明确工程文件中容易冲突的内容并制定同步规范。
2. 01 核导入库做目录隔离与命名规范，避免互相污染。
3. 提供自动检查脚本（检测遗漏文件、路径失效、重复库引用）。
4. 输出团队协作流程（提交前检查、合并后验证）。

### 17) 根据 tag 自动生成区间更新日志
**Prompt：**
请实现“两个 tag 间自动生成更新日志”的脚本/流程。要求：
1. 输入：`from_tag`、`to_tag`；输出：按功能分组的 changelog。
2. 解析 commit message 并支持过滤 merge/格式化提交。
3. 可选输出 Markdown，便于直接发布。
4. 给出 CI 或本地一键命令示例。
5. 验收：随机选两个历史 tag，生成结果与人工核对基本一致。

### 18) 库文件和工程文件自动备份/更新/打包
**Prompt：**
请设计“库文件+工程文件自动备份、更新、打包、下载即用”流程。要求：
1. 定义版本号与产物命名规则（日期+tag+短哈希）。
2. 自动收集必要文件，排除临时/中间产物。
3. 生成可校验包（hash 清单）与发布说明。
4. 输出恢复流程（下载后如何一键还原到可编译状态）。
5. 验收：新目录中按说明可一次性恢复并编译通过。

### 19) 使用 ESKF 融合 GPS 与惯导
**Prompt：**
请在现有系统中设计并实现 ESKF 融合 GPS+惯导。要求：
1. 给出状态向量、误差状态定义、系统模型与观测模型。
2. 明确 IMU/GPS 时间同步、异常值剔除、失锁处理。
3. 输出关键参数初值（噪声协方差等）与调参策略。
4. 提供离线回放验证脚本与在线指标（位置漂移、航向稳定度）。
5. 验收：与现有方案对比，定位稳定性和鲁棒性有量化提升。

### 20) 将姿态估计 EKF 优化为 ESKF
**Prompt：**
请将当前姿态 EKF 升级为 ESKF。要求：
1. 分析现有 EKF 问题（线性化误差、数值稳定性、姿态表示）。
2. 采用误差状态建模与四元数主状态更新。
3. 提供与旧 EKF 并行开关，支持 A/B 对比。
4. 输出：核心推导、关键代码、测试结果对比（漂移、抖动、收敛速度）。
5. 验收：在典型工况下 ESKF 指标优于旧 EKF。

### 21) 视觉算法表中“仅 py 完成”的任务转 C（电脑端/车端）
**Prompt：**
请将以下已完成 Python 版本的视觉算法，逐项迁移到 C（先电脑端再车端）：
- 逆透视打表算法
- PVC 视觉检测
- 颠簸路段视觉检测
- 单边桥视觉检测
- 操场道线视觉检测

统一要求：
1. 每个算法先做 C 电脑端实现，并与 Python 结果逐帧对比（精度/差异统计）。
2. 增加单帧计时、平均耗时、FPS 统计，使用宏定义控制开关。
3. 计时结果写在对应 C 文件头部注释中（标明测试机器与日期）。
4. 完成后再给出车端 C 迁移建议（内存、定点化、接口适配）。
5. 目录组织参考现有视觉工具链结构（例如 `tools/07_针对小车车载视频的cv算法/...`）。
6. 验收：C 与 Python 在测试集上结果一致性达到目标阈值，并给出性能报告。
