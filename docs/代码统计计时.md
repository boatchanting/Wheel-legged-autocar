常量必须带 f 后缀，如 9806.65f，360.0f。
使用 fabsf() 代替 fabs()。
这些小改动能让编译器充分利用单片机的硬件单精度 FPU（如 M4F/M7 内核），成倍缩短 EKF_UpData() 和运算环的耗时。

0 板子车
函数 EKF_UpData(); cnt=38230, avg=125 us, min=125 us, max=133 us, last=126 us
函数 InertialNav_Update(
                euler_angle.yaw,                                 // 当前偏航角
                9806.65*((float)imu_data.acc_y/4098-(float)imu_data.grav_y),                                // 纵向加速度 (前+)
                9806.65*((float)imu_data.grav_x-(float)imu_data.acc_x/4098), // 横向加速度 (左+) 9.80665是重力加速度，这里乘了1000倍是因为转换为mm/s^2，imu数据是4096位的，所以需要除4096
                (float)motor_value.receive_left_speed_data,      // 左轮速
                (float)motor_value.receive_right_speed_data      // 右轮速
            );
            cnt=4455, avg=2 us, min=2 us, max=4 us, last=2 us
函数 gnss_data_parse(); cnt=348, avg=5 us, min=5 us, max=13 us, last=12 us
函数 Servo_Speed_Control(target_speed_set, current_actual_speed,euler_angle.pitch); cnt=1002, avg=1 us, min=1 us, max=3 us, last=1 us