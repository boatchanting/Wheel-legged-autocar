 ✅️
【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通 C 语言、双核 MCU（CM7）架构、定时器/中断调度、RTOS 任务周期配置以及 PID 控制算法。

【任务背景】
我有一个基于双核架构的嵌入式控制工程。Core 0（主控逻辑）负责底层控制、姿态解算和执行器控制，部分数据可能通过 IPC 与 Core 1 交互。
现在我需要对 Core 0 侧的任务执行周期进行局部调整，并根据新的执行周期对已有 PID 参数做等效换算。请根据我提供的文件列表和实际代码进行最小化修改。

【具体任务指令】
请修改工程中的中断周期、定时器分频或 RTOS 任务周期配置，并根据新的周期重新调整相关 PID 参数。

当前各任务的执行周期（单位：ms）：
直立：
- speed: 20
- angle: 5
- gyro: 1

转向：
- yaw: 6
- gyro: 2
- imu: 1
- ekf: 5
- servo_control: 1

目标理想周期（单位：ms）：
直立：
- speed: 9
- angle: 3
- gyro: 1

转向：
- yaw: 3
- gyro: 1
- imu: 1
- ekf: 1
- servo_control: 1

请完成以下两项任务：

任务1：修改任务周期配置（作用于 Core 0）
涉及文件优先检查：
user\cm7_0_isr.c
user\main_cm7_0.c
code\calculate\ekf.c
code\calculate\ekf.h

修改要求：
- 找到工程中设置定时器中断、调度计数器、分频计数、RTOS 任务 delay 或任务周期宏定义的代码。
- 将上述任务的触发周期改为目标值。
- 如果多个任务由同一个基础中断周期分频得到，请保持基础中断不变，优先修改对应任务的软件分频系数或触发计数阈值。
- 如果任务使用独立定时器，请分别检查并修改对应定时器的 PSC、ARR 或周期宏定义。
- 如果某个任务的目标周期与原周期相同（如直立 gyro、转向 imu、servo_control），则保持其周期配置不变。
- 不要为了凑周期重写调度框架；只在现有调度方式内做局部调整。

任务2：根据新周期等效调整已有 PID 参数（作用于 Core 0）
涉及文件优先检查：
code\calculate\pid-new.c
code\calculate\pid-new.h
user\cm7_0_isr.c
user\main_cm7_0.c
code\calculate\ekf.c
code\calculate\ekf.h

修改要求：
- 找到与 speed、angle、yaw、gyro、ekf 等任务直接相关的已有 PID 控制器或 PID 初始化参数。
- 先确认代码中的 PID 公式属于位置式、增量式、带积分分离、带限幅或带滤波的哪一种，再决定换算方式。
- 默认假设原 PID 参数是在原周期下整定的，按周期变化比例做等效换算：
  - Kp 通常保持不变。
  - Ki 乘以「新周期 / 原周期」。
  - Kd 除以「新周期 / 原周期」。
  - 如果代码中的 Ki、Kd 已经显式乘以 dt 或除以 dt，则不要重复换算参数，应改为检查 dt/周期变量是否同步更新。
  - 对于增量式 PID、带滤波微分或积分限幅的 PID，请按实际公式推导，确保控制量在新周期下尽量等效。
- 周期变化参考比例：
  - speed: 20ms -> 9ms，比例 0.45，Ki *= 0.45，Kd /= 0.45。
  - angle: 5ms -> 3ms，比例 0.6，Ki *= 0.6，Kd /= 0.6。
  - yaw: 6ms -> 3ms，比例 0.5，Ki *= 0.5，Kd /= 0.5。
  - gyro（转向）: 2ms -> 1ms，比例 0.5，Ki *= 0.5，Kd /= 0.5。
  - ekf: 5ms -> 1ms，比例 0.2；如果 EKF 内存在 dt、噪声离散化或滤波周期配置，应同步检查，而不是简单套用 PID 规则。
  - 周期未变化的任务不修改 PID 参数。
- 如果无法从代码中可靠判断某个 PID 与任务周期的对应关系，请不要盲目修改；请在输出中明确说明未修改原因和建议确认点。

任务3：检查周期调整后的调用一致性
涉及文件：
user\cm7_0_isr.c
user\main_cm7_0.c
code\calculate\ekf.c
code\calculate\ekf.h
code\calculate\pid-new.c
code\calculate\pid-new.h

修改要求：
- 检查任务周期、PID 参数、dt 变量、滤波周期、EKF 更新时间是否存在重复定义或互相矛盾。
- 如果存在周期宏定义、注释说明或参数命名与新周期不一致，请做最小化同步更新。
- 如果发现某个目标周期受硬件定时器整数分频限制无法精确实现，请选择最接近且误差最小的配置，并在输出中说明实际周期和误差。

【🔴 严格约束条件】（必须绝对遵守）
- 禁止大改架构：只允许在原有中断、定时器、任务调度和 PID 参数初始化框架内做局部修改，绝对不要重构状态机、任务执行顺序或函数调用链。
- 禁止新增无关功能：不要增加与周期调整、PID 等效换算、dt 同步无关的代码。
- 保留所有注释：原有中文/英文注释、特殊标记（如 //TODO）必须 100% 保留，一行都不许删除；如注释中的周期值已过期，只能在原注释附近补充或最小化更新。
- 变量命名规范：新增或修改的周期宏、计数变量、PID 参数变量必须与原有代码风格保持一致。
- 周期未变化不修改：目标周期和原周期相同的任务，不修改其周期配置，也不修改其 PID 参数。
- 参数换算要可追溯：修改 PID 参数时必须说明原周期、新周期、换算比例和修改后的参数值。
- 增量输出：明确指出修改了哪个文件的哪一段代码，以及为什么需要修改。
- 编译友好：修改完成后检查头文件声明、宏定义、extern 声明和 include 关系，避免引入重复定义或未声明符号。

【提供的文件列表】
中断与调度：
user\cm7_0_isr.c
user\main_cm7_0.c

姿态解算与周期相关：
code\calculate\ekf.c
code\calculate\ekf.h

PID 参数与控制算法：
code\calculate\pid-new.c
code\calculate\pid-new.h//这里有大部分的pid参数
code\plan\bridge.h//这里有高腿姿态下的pid参数
如实际代码中周期或 PID 参数定义在其他文件，请先说明发现的位置，再做最小化修改。
