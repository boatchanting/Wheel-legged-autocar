【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通C语言、双核MCU（CM7）架构、图像处理（逆透视变换）以及PID控制算法。
【任务背景】
我有一个基于双核架构的“颠簸路段视觉识别与控制”项目。Core 1（主控视觉）负责图像处理与识别，Core 0（主控逻辑）负责底层控制，双核通过IPC通信。
现在我需要对现有代码进行两项局部功能升级，请根据我提供的文件列表和代码进行修改。
【具体任务指令】
任务1：颠簸路段视觉识别模块增加逆透视（IPM）调用 (作用于 Core 1) 
涉及文件：code1\vision\bumpy_vision.c / .h，user\main_cm7_1.c
修改要求：在 bumpy_vision.c 中引入 code1\vision\ipm_transform.h。在进行颠簸路段特征提取后，增加对逆透视（IPM）函数的调用，将原始图像坐标转换为逆透视坐标。 
任务2：颠簸路段视觉控制模块方向控制改为PID控制 (作用于 Core 0)
涉及文件：code\vision\vision_bumpy_control.c / .h，user\cm7_0_isr.c
修改要求：将现有的方向控制逻辑（可能是简单的比例控制或bang-bang控制）替换为标准的位置式或增量式PID控制算法。
在头文件中定义PID相关的结构体（包含Kp, Ki, Kd, error, last_error, integral等）。
在 .c 文件中实现PID计算，并输出平滑的方向控制量。
任务3：确认IPC通信兼容性 (连接 Core 0 与 Core 1)
涉及文件：vision_ipc.h, vision_ipc_core0.c/.h, vision_ipc_core1.c/.h
修改要求：检查因任务1和任务2的修改，双核传递的数据结构（如目标偏差值、坐标等）是否需要微调。如果需要，请在IPC相关文件中做最小化适配；如果不需要，则保持原样。
【🔴 严格约束条件】（必须绝对遵守）
禁止大改架构：只在原有的函数框架内增加IPM调用和PID算法，绝对不要重构我的代码逻辑、状态机或函数调用链。
保留所有注释：我原有的中文/英文注释、特殊标记（如 //TODO）必须100%保留，一行都不许删除。
变量命名规范：新增的PID变量或其他变量命名必须与我原有代码的风格保持完全一致。 
增量输出：明确指出修改了哪个文件的哪一段代码。 
【提供的文件列表】
逆透视：code1\vision\ipm_transform.h
视觉识别：code1\vision\bumpy_vision.h , code1\vision\bumpy_vision.c
视觉控制：code\vision\vision_bumpy_control.c , code\vision\vision_bumpy_control.h
01核ipc通信：code\vision\vision_ipc_core0.c / .h, code\vision\vision_ipc.h, code1\vision\vision_ipc_core1.c / .h
识别调度与控制调用：user\main_cm7_1.c , user\cm7_0_isr.c