## 颠簸路段控制 ✅️
【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通C语言、双核MCU（CM7）架构、图像处理（逆透视变换）以及PID控制算法。
【任务背景】
我有一个基于双核架构的“颠簸路段视觉识别与控制”项目。Core 1（主控视觉）负责图像处理与识别，Core 0（主控逻辑）负责底层控制，双核通过IPC通信。
现在我需要对现有代码进行两项局部功能升级，请根据我提供的文件列表和代码进行修改。
【具体任务指令】
任务1：颠簸路段视觉识别模块增加逆透视（IPM）调用 (作用于 Core 1) 
涉及文件：code1\vision\bumpy_vision.c / .h，user\main_cm7_1.c
修改要求：在 bumpy_vision.c 中引入 code1\vision\ipm_transform.h。在进行颠簸路段特征提取后，增加对逆透视（IPM）函数的调用，将原始图像坐标转换为逆透视坐标。 
任务2：颠簸路段视觉控制模块方向控制改为PID控制 (作用于 Core 0)
涉及文件：code\vision\vision_bumpy_control.c / .h，user\cm7_0_isr.c
修改要求：将现有的方向控制逻辑（可能是简单的比例控制或bang-bang控制）替换为标准的位置式或增量式PID控制算法。
在头文件中定义PID相关的结构体（包含Kp, Ki, Kd, error, last_error, integral等）。
在 .c 文件中实现PID计算，并输出平滑的方向控制量。
任务3：确认IPC通信兼容性 (连接 Core 0 与 Core 1)
涉及文件：vision_ipc.h, vision_ipc_core0.c/.h, vision_ipc_core1.c/.h
修改要求：检查因任务1和任务2的修改，双核传递的数据结构（如目标偏差值、坐标等）是否需要微调。如果需要，请在IPC相关文件中做最小化适配；如果不需要，则保持原样。
【🔴 严格约束条件】（必须绝对遵守）
禁止大改架构：只在原有的函数框架内增加IPM调用和PID算法，绝对不要重构我的代码逻辑、状态机或函数调用链。
保留所有注释：我原有的中文/英文注释、特殊标记（如 //TODO）必须100%保留，一行都不许删除。
变量命名规范：新增的PID变量或其他变量命名必须与我原有代码的风格保持完全一致。 
增量输出：明确指出修改了哪个文件的哪一段代码。 
【提供的文件列表】
逆透视：code1\vision\ipm_transform.h
视觉识别：code1\vision\bumpy_vision.h , code1\vision\bumpy_vision.c
视觉控制：code\vision\vision_bumpy_control.c , code\vision\vision_bumpy_control.h
01核ipc通信：code\vision\vision_ipc_core0.c / .h, code\vision\vision_ipc.h, code1\vision\vision_ipc_core1.c / .h
识别调度与控制调用：user\main_cm7_1.c , user\cm7_0_isr.c

## 单边桥路段控制
【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通C语言、双核MCU（CM7）架构、图像处理（逆透视变换）以及PID控制算法。
【任务背景】
我有一个基于双核架构的“单边桥路段视觉识别与控制”项目。Core 1（主控视觉）负责图像处理与识别，Core 0（主控逻辑）负责底层控制，双核通过IPC通信。
现在我需要对现有代码进行两项局部功能升级，请根据我提供的文件列表和代码进行修改。
【具体任务指令】
任务1：单边桥路段视觉识别模块增加逆透视（IPM）调用 (作用于 Core 1) 
涉及文件：code1\vision\line_vision.c
\.h
，user\main_cm7_1.c
修改要求：在 line_vision.c 中引入 code1\vision\ipm_transform.h。在进行单边桥路段特征提取后，增加对逆透视（IPM）函数的调用，将原始图像坐标转换为逆透视坐标。 
任务2：单边桥路段视觉控制模块方向控制改为PID控制 (作用于 Core 0)
涉及文件：code\vision\vision_bridge_control.c/.h，user\cm7_0_isr.c
修改要求：将现有的方向控制逻辑（可能是简单的比例控制或bang-bang控制）替换为标准的位置式或增量式PID控制算法。
在头文件中定义PID相关的结构体（包含Kp, Ki, Kd, error, last_error, integral等）。
在 .c 文件中实现PID计算，并输出平滑的方向控制量。
任务3：确认IPC通信兼容性 (连接 Core 0 与 Core 1)
涉及文件：vision_ipc.h, vision_ipc_core0.c/.h, vision_ipc_core1.c/.h
修改要求：检查因任务1和任务2的修改，双核传递的数据结构（如目标偏差值、坐标等）是否需要微调。如果需要，请在IPC相关文件中做最小化适配；如果不需要，则保持原样。
【🔴 严格约束条件】（必须绝对遵守）
禁止大改架构：只在原有的函数框架内增加IPM调用和PID算法，绝对不要重构我的代码逻辑、状态机或函数调用链。
保留所有注释：我原有的中文/英文注释、特殊标记（如 //TODO）必须100%保留，一行都不许删除。
变量命名规范：新增的PID变量或其他变量命名必须与我原有代码的风格保持完全一致。 
增量输出：明确指出修改了哪个文件的哪一段代码。 
【提供的文件列表】
逆透视：code1\vision\ipm_transform.h
视觉识别：
code1\vision\line_vision.c
code1\vision\line_vision.h
视觉控制：
code\vision\vision_bridge_control.c
code\vision\vision_bridge_control.h
01核ipc通信：code\vision\vision_ipc_core0.c / .h, code\vision\vision_ipc.h, code1\vision\vision_ipc_core1.c / .h
识别调度与控制调用：user\main_cm7_1.c , user\cm7_0_isr.c

## pvc路段控制
【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通C语言、双核MCU（CM7）架构、图像处理（逆透视变换）以及PID控制算法。
【任务背景】
我有一个基于双核架构的“单边桥路段视觉识别与控制”项目。Core 1（主控视觉）负责图像处理与识别，Core 0（主控逻辑）负责底层控制，双核通过IPC通信。
现在我需要对现有代码进行两项局部功能升级，请根据我提供的文件列表和代码进行修改。
【具体任务指令】
任务1：单边桥路段视觉识别模块增加逆透视（IPM）调用 (作用于 Core 1) 
涉及文件：code1\vision\pvc_vision.c
\.h
，user\main_cm7_1.c
修改要求：在 pvc_vision.c 中引入 code1\vision\ipm_transform.h。在进行单边桥路段特征提取后，增加对逆透视（IPM）函数的调用，将原始图像坐标转换为逆透视坐标。 
任务2：单边桥路段视觉控制模块方向控制改为PID控制 (作用于 Core 0)
涉及文件：code\vision\vision_pvc_control.c/.h，user\cm7_0_isr.c
修改要求：将现有的方向控制逻辑（可能是简单的比例控制或bang-bang控制）替换为标准的位置式或增量式PID控制算法。
在头文件中定义PID相关的结构体（包含Kp, Ki, Kd, error, last_error, integral等）。
在 .c 文件中实现PID计算，并输出平滑的方向控制量。
任务3：确认IPC通信兼容性 (连接 Core 0 与 Core 1)
涉及文件：vision_ipc.h, vision_ipc_core0.c/.h, vision_ipc_core1.c/.h
修改要求：检查因任务1和任务2的修改，双核传递的数据结构（如目标偏差值、坐标等）是否需要微调。如果需要，请在IPC相关文件中做最小化适配；如果不需要，则保持原样。
【🔴 严格约束条件】（必须绝对遵守）
禁止大改架构：只在原有的函数框架内增加IPM调用和PID算法，绝对不要重构我的代码逻辑、状态机或函数调用链。
保留所有注释：我原有的中文/英文注释、特殊标记（如 //TODO）必须100%保留，一行都不许删除。
变量命名规范：新增的PID变量或其他变量命名必须与我原有代码的风格保持完全一致。 
增量输出：明确指出修改了哪个文件的哪一段代码。 
【提供的文件列表】
逆透视：code1\vision\ipm_transform.h
视觉识别：
code1\vision\pvc_vision.c
code1\vision\pvc_vision.h
视觉控制：
code\vision\vision_pvc_control.c
code\vision\vision_pvc_control.h
01核ipc通信：code\vision\vision_ipc_core0.c / .h, code\vision\vision_ipc.h, code1\vision\vision_ipc_core1.c / .h
识别调度与控制调用：user\main_cm7_1.c , user\cm7_0_isr.c

## 斜坡路段控制 
【角色设定】
你是一位资深的嵌入式软件工程师和自动控制专家，精通C语言、双核MCU（CM7）架构、图像处理（逆透视变换）以及PID控制算法。
【任务背景】
我有一个基于双核架构的“斜坡路段视觉识别与控制”项目。Core 1（主控视觉）负责图像处理与识别，Core 0（主控逻辑）负责底层控制，双核通过IPC通信。
现在我需要增加一段代码code\vision\vision_slope_control.c .h，请根据我提供的文件列表和代码进行修改。

状态机实现学习code\vision\vision_bridge_control.h
code\vision\vision_bridge_control.c 的思路，外部调用模仿下面代码，通过全局变量
vision_detected_slope_point调用，状态机触发后自动清0
```c
// 模拟视觉触发单边桥正式任务
if (vision_detected_bridge_point == 1) 
{
    // 判断当前是否处于空闲状态，防止任务中途重复触发打断动作
    if (!VisionBridgeTask_IsActive()) 
    {
        VisionBridgeTask_Start(); // 启动正式单边桥视觉任务：先 PVC 进门，再巡线找桥
    }
    vision_detected_bridge_point = 0; // 清除标志位，避免重复触发
}
```
【具体任务指令】
斜坡上坡再下坡是一个先白色pvc斜坡，再蓝色（不识别），再白色平地的过程
控制方案：斜坡上坡状态调用触发后，先维持速度在 -600.0f(可以通过宏定义修改)，同时触发pvc校准，进入斜坡，进入斜坡后pvc超过一个阈值90%，锁角向前开，保持速度不变300ms后，降低速度为-400.0f，2000mm后自动退出斜坡状态机

【🔴 严格约束条件】（必须绝对遵守）
禁止大改架构：只在原有的文件框架内增加算法，绝对不要重构我的代码逻辑或函数调用链。
注释：我原有的中文/英文注释、特殊标记（如 //TODO）必须100%保留，一行都不许删除。新增代码注释详细
变量命名规范：新增的变量或其他变量命名必须与我原有代码的风格保持完全一致。 
【提供的文件列表】
初始化：
user\main0\init_main0.c
user\main0\init_main0.h
视觉识别：
code1\vision\pvc_vision.c
code1\vision\pvc_vision.h
视觉控制：
code\vision\vision_pvc_control.c
code\vision\vision_pvc_control.h
code\vision\vision_bridge_control.c
code\vision\vision_bridge_control.h
惯性导航：
code\navigation\inertial_nav.h
识别调度与控制调用：user\main_cm7_1.c(280-320行，状态机触发) , user\cm7_0_isr.c (286-881行为中断控制主函数，不要读完，有需要再读，291-300行为状态机控制调用)

写完之后在沙箱外跑编译命令
在沙箱外跑"D:\Softwares\iar\common\bin\iarbuild.exe" ".\iar\project_config\cyt4bb7_cm_7_0.ewp" -make Debug  
 "D:\Softwares\iar\common\bin\iarbuild.exe" ".\iar\project_config\cyt4bb7_cm_7_1.ewp" -make Debug


 写完之后在沙箱外跑编译命令
在沙箱外跑
 "C:\Program Files\IAR Systems\Embedded Workbench 9.2\common\bin\iarbuild.exe" ".\iar\project_config\cyt4bb7_cm_7_0.ewp" -make Debug  
 "C:\Program Files\IAR Systems\Embedded Workbench 9.2\common\bin\iarbuild.exe" ".\iar\project_config\cyt4bb7_cm_7_1.ewp" -make Debug
