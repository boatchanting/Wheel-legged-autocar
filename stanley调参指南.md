# Stanley 循迹控制算法调参指南 (两轮平衡车专版)

将算法由纯追踪升级为 Stanley 算法后，由于 Stanley 是强依赖**横向偏差（CTE）**与**航向误差（Heading Error）**的高阶控制逻辑，且您的车辆是**通过左右轮差速来控制航向的两轮平衡车**，在调参逻辑上与传统的舵机车有所不同。

以下是您需要重点关注的参数以及推荐的标准化调参步骤。所有的核心参数均位于 `code/navigation/nav_replay/plan1/plan1_stanley_tracking.h` 文件中。

---

## 一、 核心调参清单

在 `plan1_stanley_tracking.h` 中，以下参数是您在实车联调时最需要关注的：

### 1. 横向误差增益：`STANLEY_K` (最核心参数)
*   **默认值**：`2.5f`
*   **作用**：决定了小车**偏离轨迹时，向轨迹靠拢的“用力程度”**。
*   **特性**：
    *   **调大**：小车会非常紧贴标定的轨迹线，即使受到外力推离也能迅速拉回。但如果过大，平衡车的左右差速环会出现高频的左右震荡（俗称“画龙”或“打摆子”）。
    *   **调小**：小车走线会更加柔和、平顺，但在高速过弯时可能会轻微向外侧“漂移”（类似纯追踪的切弯现象）。

### 2. 曲率前馈增益：`K_FF_CURVATURE`
*   **默认值**：`100.0f`（延续自原纯追踪参数）
*   **作用**：根据前方路径的“弯曲程度”和当前车速，**提前给出差速指令**。
*   **特性**：这是为了抵消差速底盘的转动惯量延迟。如果没有前馈，车子在入弯时总会“慢半拍”导致甩出弯道。前馈负责“预判”，Stanley 负责“兜底纠偏”。

### 3. 角度输出滤波参数：`FILTER_ALPHA_ANGLE` 与 `SLEW_RATE_ANGLE`
*   **作用**：限制瞬间给到底层的 `err_degree` （虚拟差速偏航角）的变化率，防止平衡车因为突发的大转向指令发生倒伏或剧烈抖动。
*   **建议**：初期调试保持默认即可。如果发现直道走得不够平滑，可以适当减小 `FILTER_ALPHA_ANGLE`（比如从 `0.45f` 降到 `0.3f`）；如果嫌车身反应太迟钝，可以提高。

### 4. 弯道旁路阈值：`KAPPA_CURVE_BYPASS_THRESH`
*   **默认值**：`0.0005f`
*   **作用**：当遇到急弯时（前方曲率大于此值），系统会**临时关掉角度滤波器**，直接把最原始、最敏捷的转向指令灌给差速环，保证急弯的敏捷性。

---

## 二、 实车标准调参步骤 (Step-by-Step)

为了防止多参数耦合导致车子失控或现象难以分析，请**严格按照以下四个步骤逐步解耦调参**：

> [!CAUTION]
> **下地前请先将车速限制在安全且稳定的范围内（例如 1.0m/s - 1.5m/s），等走线完美后再推高极限速度。**

### 第一步：验证纯航向极性 (解除武装)
1.  **修改代码**：将 `STANLEY_K` 设为 `0.0f`，将 `K_FF_CURVATURE` 设为 `0.0f`。
2.  **上车表现**：此时 Stanley 退化为了纯粹的“航向角跟随（Heading Control）”，小车只关心车头朝向，不关心自己是否压在线上。
3.  **判断标准**：让车跑起来，车子大概率会偏离轨道，但**车头方向必须始终顺着赛道大致方向走**。如果车子一启动就在原地疯狂打转，说明底盘偏航极性错乱（目前理论推导无误，此步仅为防呆验证）。

### 第二步：验证横向纠偏极性 (引入 CTE)
1.  **修改代码**：将 `STANLEY_K` 设为 `1.0f`（前馈 `K_FF_CURVATURE` 依然为 0）。
2.  **上车表现**：此时小车应该具备了“寻线”能力。
3.  **人工干扰**：在直道运行时，用脚或手把车子强行往左侧推偏 20 厘米。
4.  **判断标准**：观察车子是不是主动**向右转向**去寻找轨迹。如果车子成功收敛回到轨迹，说明我们的数学符号完全正确！如果车子反而加速向左侧冲出赛道（发散），说明差速极性有暗坑，需要联系我调整底层符号。

### 第三步：推高横向增益上限 (压榨性能)
1.  **操作**：在保证第二步收敛的前提下，开始以 `0.5` 为步进逐渐推高 `STANLEY_K`。
    *   例如：`1.0` $\rightarrow$ `1.5` $\rightarrow$ `2.0` $\rightarrow$ `2.5` $\rightarrow$ `3.0`
2.  **判断标准**：一直推高，直到小车在直道匀速行驶时，能够肉眼观察到车身在轨迹线上发生**高频的左右微小扭动（差速画龙）**。
3.  **设定最终值**：记录下刚开始画龙的这个临界值（假设为 3.0），**将其回调 20%** （即 $3.0 \times 0.8 = 2.4$），这就是当前车速和底盘机械特性下的最优 `STANLEY_K`！

### 第四步：调教弯道前馈 (消除滞后)
1.  **恢复前馈**：将 `STANLEY_K` 设置为您在第三步找到的最优值，然后将 `K_FF_CURVATURE` 恢复为 `100.0f`（或以前纯追踪用得很顺手的值）。
2.  **观察过弯表现**：挑选赛道上最急的一个 U 型弯或 S 弯进行观察。
    *   **现象A (入弯滞后，出弯向外甩)**：说明差速预判给晚了/给少了。**解决：调大 `K_FF_CURVATURE`**（例如加到 120），或者略微调大一点 `ahead_idx` 的偏置常量（目前是 +15 点）。
    *   **现象B (入弯太早，甚至切内弯压内角)**：说明前馈给得太大太猛。**解决：调小 `K_FF_CURVATURE`**（例如降到 80）。
    *   **现象C (完美贴弯，犹如轨道车)**：调参完成！

## 三、 常见疑难杂症 (Q&A)

> [!TIP]
> **Q: 车子直道走得很稳，一到高速弯就甩出去了，怎么办？**
> **A:** 高速过弯的离心力很大。首要手段是加大 `K_FF_CURVATURE` 让车更早、更主动地发力入弯；其次，如果弯道处离线速度规划 `target_speed` 降得不够低，也会因为超出了轮胎/电机的物理极限而甩出。建议在离线 `chazhi.py` 中把过弯限速压低。

> [!WARNING]
> **Q: 小车在某些轻微弧线处突然抽搐一下（差速指令突变）？**
> **A:** 这可能是由于曲率毛刺触发了 `KAPPA_CURVE_BYPASS_THRESH`，导致角度滤波器突然被旁路。您可以尝试略微调大 `KAPPA_CURVE_BYPASS_THRESH`（例如从 0.0005 调到 0.001），让它只在真正的急弯才旁路滤波器；或者重新在上位机优化打点的平滑度，防止生成曲率毛刺。
