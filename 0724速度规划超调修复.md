# 0724 速度规划超调修复

**日期**：2026-07-24  
**问题来源**：遥测日志 `wifi_telemetry_20260723_225734_加入舵机相关.csv`  
**关联分析**：`遥测日志分析 (加入舵机相关量) - 2026-07-23.md`

---

## 问题现象

目标速度从 0 阶跃到 -626 RPM 时，实际速度冲到 -874 RPM，超调约 40%。  
超调触发后舵机速度环全功率反制仍无法及时收敛，导致车身大幅前倾后急刹。

## 根因分析

1. **加速段不限斜率**：`NavReplay_SpeedSlew_Update()` 中，当检测到"同向且目标速度上升"时，直接 `return raw_speed` 跳过了斜率限制，导致目标速度瞬间阶跃。
2. **加速前馈未启用**：`ACCEL_FF_ENABLE` 为 0，不存在前馈补偿，所有加速力矩完全依赖 PID 误差驱动，误差突变必然导致过冲。
3. **BRAKE 档限幅偏低**：急刹时 PID 输出限幅 2300 不足以快速收敛高速超调。

---

## 修改内容

### P0：修复加速段不限斜率的 bug

**涉及文件**（3 个）：

| 文件 | 修改 |
|---|---|
| `code/navigation/nav_replay/plan1/plan1_lqr_tracking.c` | 删除 `return raw_speed` 直接放行分支 |
| `code/navigation/nav_replay/plan1/plan1_pure_pursuit_speed_planning.c` | 同上 |
| `code/navigation/nav_replay/plan1/plan1_pure_pursuit.c` | 同上 |

**改前**：
```c
// 加速段直接给目标速度，保留目标速度台阶，避免把加速前馈的触发条件抹平。
if (((raw_speed * s_prev_speed_set) >= 0.0f) &&
    (abs_raw > (abs_prev + NAV_SPEED_SLEW_EPS)))
{
    return raw_speed;  // ← 问题所在：直接放行，无斜率限制
}
```

**改后**：
```c
// 加速段也走斜率限制，避免目标速度阶跃导致舵机速度环大幅超调。
// （删除整个 if 块，让加速段自然落入下方 step_limit 分支）
```

### P1：放大正常加速步长 NAV_SPEED_SLEW_UP_NORMAL

**涉及文件**（3 个）：

| 文件 | 改前 | 改后 |
|---|---|---|
| `code/navigation/nav_replay/plan1/plan1_lqr_tracking.h` | `45.0f` | `70.0f` |
| `code/navigation/nav_replay/plan1/plan1_pure_pursuit_speed_planning.h` | `45.0f` | `70.0f` |
| `code/navigation/nav_replay/plan1/plan1_pure_pursuit.h` | `45.0f` | `70.0f` |

**理由**：P0 修复后加速段会走斜率限制，原值 45 RPM/周期偏保守（0→626 需 ~14 个周期 ≈ 126ms），放大到 70 后约 9 个周期 ≈ 81ms，在安全框架内保持合理的加速响应。

### P4：BRAKE 档舵机速度环输出限幅放大

**涉及文件**：`code/calculate/pid-new.c`

| 参数 | 改前 | 改后 |
|---|---|---|
| `g_control_profile_brake.servo_speed_max_output` | `2300.0f` | `2600.0f` |

**理由**：与 ACCEL 档持平，给急刹时速度环更大的纠偏权限，有助于刹车阶段更快收敛实际速度。

---

## 未采纳的方案

| 方案 | 原因 |
|---|---|
| 启用加速前馈 (`ACCEL_FF_ENABLE = 1`) | 需要额外调参（增益、限幅、斜率），风险较高，暂不启用 |

---

## 预期效果

- 目标速度从阶跃变为斜坡（~70 RPM/周期），舵机速度环有足够时间跟随
- 超调幅度从 ~40% 降至 <10%
- 急刹时 PID 输出限幅放宽，刹车收敛更快

## 调参建议

若实测后加速响应偏慢，可适当调大 `NAV_SPEED_SLEW_UP_NORMAL`（建议范围 60~100）。  
若刹车仍不够果断，可进一步调大 BRAKE 档的 `servo_speed_max_output`（建议不超过 3000）。
